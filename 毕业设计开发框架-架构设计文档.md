# 大学计算机专业毕业设计开发框架（GP-Framework）

> **版本**：v1.0
> **定位**：一套可长期复用的企业级后台开发框架，作为所有本科毕业设计的"母版"
> **核心理念**：**一次搭建，N 次复用 —— 新系统只需新增业务模块即可完成**

---

## 📖 目录

- [一、项目总体架构](#一项目总体架构)
- [二、后台基础模块](#二后台基础模块)
- [三、业务模块规范](#三业务模块规范)
- [四、页面模板（Vue3 + Element Plus）](#四页面模板vue3--element-plus)
- [五、数据库模板](#五数据库模板)
- [六、代码模板](#六代码模板)
- [七、系统开发流程](#七系统开发流程)
- [八、可插拔业务模块](#八可插拔业务模块)
- [九、系统 UI 规范](#九系统-ui-规范)
- [十、毕业设计规范](#十毕业设计规范)

---

## 文档说明

| 项目 | 说明 |
|------|------|
| 框架名称 | GP-Framework（Graduation Project Framework） |
| 后端技术 | Spring Boot 2.7 + Spring Security + JWT + MyBatis Plus 3.5 + MySQL 8.0 + Redis |
| 前端技术 | Vue 3 + Vite 4 + Element Plus + Pinia + Vue Router 4 + Axios + ECharts |
| 设计参考 | 借鉴若依（RuoYi）的核心思想，但自主精简设计，代码量适中，适合答辩讲解 |
| 适用场景 | 学生管理、宿舍管理、图书馆、医院、物流、企业 OA、酒店、商城、宠物寄养等所有后台管理系统毕设 |
| 许可 | 自由使用、修改、用于毕业设计 |

### 设计原则总览

| 原则 | 说明 |
|------|------|
| **三层架构** | Controller → Service → Mapper，职责清晰，符合软件工程规范 |
| **MVC 思想** | 前端 View + 后端 Controller + 数据 Model 分离 |
| **RBAC 权限** | 用户-角色-菜单-权限，最小粒度到按钮 |
| **高内聚低耦合** | 基础模块与业务模块完全分离，业务模块之间互不依赖 |
| **数据库三范式** | 主键唯一、字段原子性、消除传递依赖 |
| **可插拔** | 新增业务模块 = 新增一个包 + 一张表 + 一组页面，无需改框架 |
| **代码生成器** | 根据表结构一键生成 Entity/Mapper/Service/Controller/前端页面 |
| **统一规范** | 统一返回、统一分页、统一异常、统一日志、统一命名 |

---

## 一、项目总体架构

### 1.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        浏 览 器 / 移 动 端                         │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTPS / JWT Token
                     ┌───────▼───────┐
                     │   Nginx 反代   │  (前端静态资源 + 反向代理后端)
                     └───┬───────┬───┘
            ┌────────────┘       └────────────┐
     ┌──────▼──────┐                    ┌──────▼──────┐
     │  前端 Vue3   │                    │  后端 API   │
     │  Vite 构建   │                    │  Spring Boot│
     │  Element Plus│                    │  RESTful    │
     └─────────────┘                    └──────┬──────┘
                                             │
                    ┌────────────────────────┼────────────────────────┐
                    │                        │                        │
             ┌──────▼──────┐          ┌──────▼──────┐         ┌──────▼──────┐
             │   Spring     │          │  MyBatis    │         │   Spring    │
             │  Security    │          │    Plus     │         │   Task      │
             │  + JWT 认证   │          │  (ORM层)    │         │  (定时任务)  │
             └─────────────┘          └──────┬──────┘         └─────────────┘
                                             │
                                     ┌───────▼───────┐
                                     │  Druid 连接池  │
                                     └───────┬───────┘
                                             │
              ┌──────────────────────────────┼──────────────────────────────┐
              │                              │                              │
       ┌──────▼──────┐               ┌───────▼───────┐              ┌──────▼──────┐
       │  MySQL 8.0  │               │    Redis 6    │              │  本地/OSS   │
       │  (业务数据)  │               │  (缓存/Token) │              │  (文件存储)  │
       └─────────────┘               └───────────────┘              └─────────────┘
```

### 1.2 前后端分离架构

```
┌─────────────── 前端（Frontend） ───────────────┐     ┌──────── 后端（Backend） ────────┐
│                                                │     │                                 │
│  ┌─────────┐  ┌─────────┐  ┌──────────┐        │     │  ┌───────────┐                  │
│  │  视图层  │  │ 路由层  │  │ 状态管理 │        │     │  │Controller │ ← 接收请求/返回JSON │
│  │ Views   │  │ Router  │  │  Pinia   │        │     │  └─────┬─────┘                  │
│  │ .vue    │  │         │  │          │        │     │        │                        │
│  └────┬────┘  └─────────┘  └────┬─────┘        │     │  ┌─────▼─────┐                  │
│       │                        │              │     │  │  Service  │ ← 业务逻辑         │
│  ┌────▼────────────────────────▼────┐         │     │  └─────┬─────┘                  │
│  │     组件库 Element Plus           │         │     │        │                        │
│  └───────────────────────────────────┘        │     │  ┌─────▼─────┐                  │
│                                               │     │  │   Mapper  │ ← 数据访问        │
│  ┌─────────────────────────────────┐          │     │  └─────┬─────┘                  │
│  │  Axios HTTP 封装（请求/响应拦截） │ ◄──────► │     │        │                        │
│  └─────────────────────────────────┘   JSON   │     │  ┌─────▼─────┐                  │
│                                               │     │  │  MySQL    │                  │
└───────────────────────────────────────────────┘     │  └───────────┘                  │
                                                      └─────────────────────────────────┘
```

**分离规范：**
- 前端只负责 UI 渲染、表单校验、路由控制；不包含任何业务逻辑
- 后端只负责数据处理、业务逻辑、权限校验；返回统一的 JSON（`Result<T>`）
- 前后端通过 **RESTful API** 通信，使用 **JWT Token** 进行身份认证
- 接口文档由 **Knife4j（Swagger 增强）** 自动生成

### 1.3 技术栈清单

#### 后端技术栈

| 分类 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 基础框架 | Spring Boot | 2.7.18 | 自动配置、内嵌 Tomcat |
| 安全框架 | Spring Security | 5.7 | 认证授权 |
| Token | JJWT | 0.11.5 | JWT 生成与解析 |
| ORM | MyBatis Plus | 3.5.3.1 | 增强版 MyBatis，自带 CRUD |
| 数据库 | MySQL | 8.0 | 关系型数据库 |
| 连接池 | Druid | 1.2.16 | 数据库连接池 + SQL 监控 |
| 缓存 | Redis | 6.x | Token 缓存、字典缓存、限流 |
| 接口文档 | Knife4j | 4.1.0 | Swagger 增强 UI |
| Excel | EasyExcel | 3.3.2 | 导入导出 |
| 对象映射 | MapStruct | 1.5.5 | Entity ↔ DTO/VO 转换 |
| 工具库 | Hutool | 5.8.22 | 通用工具类 |
| 参数校验 | Hibernate Validator | 6.2 | `@Valid` 参数校验 |
| 定时任务 | Spring Scheduling / Quartz | - | 定时任务调度 |
| Lombok | Lombok | 1.18.30 | 简化 POJO |

#### 前端技术栈

| 分类 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 核心框架 | Vue | 3.4 | 组合式 API（Composition API） |
| 构建工具 | Vite | 4.5 | 极速热更新 |
| UI 组件 | Element Plus | 2.5 | 饿了么 Vue3 组件库 |
| 状态管理 | Pinia | 2.1 | Vue3 推荐的状态管理 |
| 路由 | Vue Router | 4.2 | 前端路由 |
| HTTP | Axios | 1.6 | 请求封装 |
| 图表 | ECharts | 5.4 | 数据可视化 |
| 富文本 | wangEditor | 5.1 | 富文本编辑器 |
| HTTP 工具 | qs | 6.11 | 参数序列化 |

### 1.4 项目目录结构

#### 后端目录结构（单模块多包，推荐毕设使用）

```
gp-admin/                                    # 后端项目根目录
├── pom.xml                                  # Maven 配置
├── src/main/java/com/gp/
│   ├── GpApplication.java                   # ★ 启动类
│   │
│   ├── common/                              # 【公共模块】所有模块共享
│   │   ├── annotation/                      #   自定义注解
│   │   │   ├── Anonymous.java                #     匿名访问注解
│   │   │   ├── DataScope.java                #     数据权限注解
│   │   │   ├── Log.java                      #     操作日志注解
│   │   │   └── RateLimiter.java              #     接口限流注解
│   │   ├── config/                           #   通用配置
│   │   ├── constant/                         #   常量
│   │   │   ├── CacheConstant.java            #     缓存 Key 前缀
│   │   │   ├── CommonConstant.java           #     系统常量
│   │   │   └── SecurityConstant.java         #     安全常量
│   │   ├── enums/                            #   枚举
│   │   │   ├── BusinessTypeEnum.java         #     业务操作类型
│   │   │   ├── HttpStatusEnum.java           #     HTTP 状态
│   │   │   └── ResultCodeEnum.java           #     返回码
│   │   ├── exception/                        #   异常
│   │   │   ├── BusinessException.java        #     业务异常
│   │   │   ├── CaptchaException.java         #     验证码异常
│   │   │   └── ServiceRuntimeException.java  #     服务异常
│   │   ├── core/                             #   ★ 核心基础类
│   │   │   ├── page/                         #     分页相关
│   │   │   │   ├── PageQuery.java            #       分页查询基类
│   │   │   │   └── PageResult.java           #       分页结果
│   │   │   ├── result/                       #     统一返回
│   │   │   │   └── Result.java               #       ★ 统一返回结果
│   │   │   ├── entity/                       #     实体基类
│   │   │   │   └── BaseEntity.java           #       ★ 实体基类（含审计字段）
│   │   │   └── controller/                   #     Controller 基类
│   │   │       └── BaseController.java       #       Controller 基类
│   │   ├── handler/                          #   全局处理器
│   │   │   ├── GlobalExceptionHandler.java   #     ★ 全局异常处理
│   │   │   └── MyMetaObjectHandler.java      #     ★ 自动填充处理器
│   │   └── utils/                            #   工具类
│   │       ├── JwtUtils.java                 #     JWT 工具
│   │       ├── RedisUtils.java               #     Redis 工具
│   │       ├── SecurityUtils.java            #     安全工具（获取当前用户）
│   │       └── SpringUtils.java              #     Spring 容器工具
│   │
│   ├── framework/                            # 【框架配置模块】
│   │   ├── config/                           #   配置类
│   │   │   ├── SecurityConfig.java           #     ★ Security 安全配置
│   │   │   ├── RedisConfig.java              #     ★ Redis 配置
│   │   │   ├── MybatisPlusConfig.java        #     ★ MyBatis Plus 配置
│   │   │   ├── CorsConfig.java               #     跨域配置
│   │   │   ├── Knife4jConfig.java            #     接口文档配置
│   │   │   └── WebMvcConfig.java             #     Web MVC 配置
│   │   ├── interceptor/                      #   拦截器
│   │   │   ├── JwtAuthInterceptor.java       #     JWT 认证拦截器
│   │   │   └── RepeatSubmitInterceptor.java  #     防重复提交
│   │   ├── filter/                           #   过滤器
│   │   │   ├── JwtAuthFilter.java            #     ★ JWT 认证过滤器
│   │   │   └── XssFilter.java                #     XSS 过滤
│   │   ├── security/                         #   安全核心
│   │   │   ├── UserDetailsServiceImpl.java   #     用户认证实现
│   │   │   ├── JwtAuthenticationProvider.java#     认证提供者
│   │   │   └── AuthenticationEntryPoint.java #     未认证处理
│   │   └── aspectj/                          #   AOP 切面
│   │       ├── LogAspect.java                #     ★ 操作日志切面
│   │       ├── DataScopeAspect.java          #     数据权限切面
│   │       └── RateLimiterAspect.java        #     限流切面
│   │
│   ├── system/                               # 【系统管理模块】
│   │   ├── domain/                           #   实体
│   │   │   ├── SysUser.java                  #     用户
│   │   │   ├── SysRole.java                  #     角色
│   │   │   ├── SysMenu.java                  #     菜单
│   │   │   ├── SysDept.java                  #     部门
│   │   │   ├── SysPost.java                  #     岗位
│   │   │   ├── SysDict.java                  #     字典
│   │   │   ├── SysConfig.java                #     参数配置
│   │   │   ├── SysOperLog.java               #     操作日志
│   │   │   ├── SysLoginInfo.java             #     登录日志
│   │   │   └── SysNotice.java                #     通知公告
│   │   ├── mapper/                           #   Mapper 接口
│   │   ├── service/                          #   Service 接口 + Impl
│   │   └── controller/                       #   Controller
│   │       ├── SysLoginController.java       #     登录/登出/验证码
│   │       ├── SysUserController.java        #     用户管理
│   │       ├── SysRoleController.java        #     角色管理
│   │       ├── SysMenuController.java        #     菜单管理
│   │       ├── SysDeptController.java        #     部门管理
│   │       ├── SysDictController.java        #     字典管理
│   │       └── SysProfileController.java     #     个人中心
│   │
│   ├── business/                             # ★【业务模块】可插拔！
│   │   ├── student/                          #   示例：学生管理
│   │   │   ├── domain/
│   │   │   │   ├── Student.java              #     实体
│   │   │   │   ├── StudentDTO.java           #     新增/修改入参
│   │   │   │   ├── StudentVO.java            #     详情/列表出参
│   │   │   │   └── StudentQuery.java         #     查询条件
│   │   │   ├── mapper/
│   │   │   │   ├── StudentMapper.java        #     Mapper 接口
│   │   │   │   └── xml/StudentMapper.xml     #     Mapper XML
│   │   │   ├── service/
│   │   │   │   ├── StudentService.java       #     Service 接口
│   │   │   │   └── impl/
│   │   │   │       └── StudentServiceImpl.java
│   │   │   ├── controller/
│   │   │   │   └── StudentController.java    #     Controller
│   │   │   ├── convert/
│   │   │   │   └── StudentConvert.java       #     对象转换（MapStruct）
│   │   │   └── enums/
│   │   │       └── StudentStatusEnum.java    #     业务枚举
│   │   │
│   │   └── [其他业务模块...]                  #   新系统只需加包即可
│   │
│   └── monitor/                              # 【监控模块】
│       ├── controller/
│       │   ├── ServerController.java         #   服务器监控
│       │   └── CacheController.java          #   Redis 缓存监控
│       └── domain/
│           └── Server.java                   #   服务器信息实体
│
├── src/main/resources/
│   ├── application.yml                       # 主配置
│   ├── application-dev.yml                   # 开发环境
│   ├── application-prod.yml                  # 生产环境
│   ├── mapper/                               # MyBatis XML
│   │   ├── system/
│   │   └── business/
│   ├── i18n/                                 # 国际化
│   └── logback-spring.xml                    # 日志配置
│
└── sql/
    ├── gp_framework.sql                      # 框架基础表（系统模块）
    └── gp_business.sql                       # 业务表（按需添加）
```

#### 前端目录结构

```
gp-ui/                                        # 前端项目根目录
├── package.json
├── vite.config.js
├── index.html
├── src/
│   ├── main.js                               # ★ 入口
│   ├── App.vue                               # ★ 根组件
│   │
│   ├── api/                                  # 【API 请求层】
│   │   ├── system/                           #   系统模块 API
│   │   │   ├── user.js
│   │   │   ├── role.js
│   │   │   ├── menu.js
│   │   │   └── dept.js
│   │   ├── business/                         #   业务模块 API
│   │   │   └── student.js
│   │   └── common/                           #   公共 API
│   │       └── login.js
│   │
│   ├── views/                                # 【页面视图层】
│   │   ├── login/                            #   登录页
│   │   │   └── index.vue
│   │   ├── layout/                           #   布局组件
│   │   │   ├── index.vue                     #     主布局
│   │   │   ├── components/
│   │   │   │   ├── Sidebar.vue               #     侧边菜单
│   │   │   │   ├── Navbar.vue                #     顶部导航
│   │   │   │   ├── TagsView.vue              #     标签页
│   │   │   │   └── Breadcrumb.vue            #     面包屑
│   │   ├── system/                           #   系统管理页面
│   │   │   ├── user/index.vue                #     用户管理
│   │   │   ├── role/index.vue                #     角色管理
│   │   │   ├── menu/index.vue                #     菜单管理
│   │   │   ├── dept/index.vue                #     部门管理
│   │   │   ├── dict/index.vue                #     字典管理
│   │   │   ├── config/index.vue              #     参数配置
│   │   │   ├── log/                          #     日志管理
│   │   │   │   ├── oper.vue                  #       操作日志
│   │   │   │   └── logininfor.vue            #       登录日志
│   │   │   └── notice/index.vue              #     通知公告
│   │   ├── business/                         #   ★ 业务模块页面
│   │   │   └── student/index.vue             #     学生管理
│   │   ├── monitor/                          #   系统监控
│   │   │   ├── server/index.vue              #     服务器监控
│   │   │   └── cache/index.vue               #     缓存监控
│   │   └── dashboard/                        #   首页
│   │       └── index.vue
│   │
│   ├── components/                           # 【公共组件层】
│   │   ├── CrmTable/                         #   通用表格（列表/分页/排序）
│   │   ├── CrmForm/                          #   通用表单
│   │   ├── CrmDialog/                        #   通用弹窗
│   │   ├── CrmSearch/                        #   高级搜索
│   │   ├── FileUpload/                       #   文件上传
│   │   ├── ImageUpload/                      #   图片上传
│   │   ├── RichEditor/                       #   富文本编辑器
│   │   ├── TreeSelect/                       #   树形选择
│   │   ├── DictTag/                          #   字典标签
│   │   ├── IconSelect/                       #   图标选择
│   │   ├── RightPanel/                       #   右侧设置面板
│   │   └── SvgIcon/                          #   SVG 图标
│   │
│   ├── router/                               # 【路由层】
│   │   └── index.js                          #   路由配置（动态路由）
│   │
│   ├── store/                                # 【状态管理层】Pinia
│   │   ├── index.js
│   │   ├── modules/
│   │   │   ├── user.js                       #   用户状态（Token/信息）
│   │   │   ├── permission.js                 #   权限路由
│   │   │   ├── app.js                        #   应用状态（侧边栏开关）
│   │   │   └── dict.js                       #   字典缓存
│   │
│   ├── utils/                                # 【工具层】
│   │   ├── request.js                        #   ★ Axios 封装
│   │   ├── auth.js                           #   Token 存取工具
│   │   ├── dict.js                           #   字典工具
│   │   ├── permission.js                     #   权限校验
│   │   ├── scroll-to.js                      #   平滑滚动
│   │   └── validate.js                       #   校验工具
│   │
│   ├── directive/                            # 【自定义指令】
│   │   ├── permission/                       #   权限指令 v-hasPermi
│   │   └── dialog/                           #   弹窗拖拽
│   │
│   ├── assets/                               # 静态资源
│   ├── styles/                               # 全局样式
│   │   ├── index.scss                        #   全局样式入口
│   │   ├── variables.scss                    #   SCSS 变量（颜色/字体）
│   │   ├── element-ui.scss                   #   Element Plus 覆盖样式
│   │   └── mixins.scss                       #   混入
│   └── settings.js                           # 系统配置（标题/主题等）
│
└── public/
    └── favicon.ico
```

### 1.5 模块划分

```
┌──────────────────────────────────────────────────────────┐
│                    GP-Framework 模块全景                    │
├──────────────────┬───────────────────────────────────────┤
│                  │  用户管理 / 角色管理 / 菜单管理          │
│   系统管理模块    │  部门管理 / 岗位管理 / 字典管理          │
│   (system)       │  参数配置 / 通知公告 / 操作日志          │
│                  │  登录日志 / 在线用户 / 定时任务          │
├──────────────────┼───────────────────────────────────────┤
│   权限安全模块    │  JWT 认证 / RBAC 授权 / 数据权限        │
│   (framework)    │  XSS 防护 / 防重复提交 / 接口限流        │
├──────────────────┼───────────────────────────────────────┤
│   公共基础模块    │  统一返回 / 统一分页 / 统一异常          │
│   (common)       │  工具类 / 常量 / 枚举 / 基类            │
├──────────────────┼───────────────────────────────────────┤
│   ★ 业务模块     │  学生管理 / 图书管理 / 宿舍管理          │
│   (business)     │  ← 新系统只需替换此模块                 │
│                  │  车辆管理 / 宠物管理 / 订单管理 ...      │
├──────────────────┼───────────────────────────────────────┤
│   系统监控模块    │  服务器监控 / 缓存监控 / SQL 监控        │
│   (monitor)      │  接口文档 / 表结构文档                  │
├──────────────────┼───────────────────────────────────────┤
│   代码生成模块    │  根据表结构 → 一键生成前后端代码          │
│   (generator)    │  Entity / Mapper / Service / Vue       │
└──────────────────┴───────────────────────────────────────┘
```

### 1.6 统一命名规范

#### 后端命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 项目名 | 小写字母，中划线分隔 | `gp-admin`、`gp-ui` |
| 包名 | 全小写，单数 | `com.gp.system` |
| 类名 | 大驼峰 | `StudentController`、`SysUserService` |
| 接口名 | 大驼峰，不以 I 开头 | `StudentService`（而非 `IStudentService`） |
| 实现类 | 大驼峰 + Impl 后缀 | `StudentServiceImpl` |
| 方法名 | 小驼峰，动词开头 | `getStudentById()`、`insertStudent()` |
| 变量名 | 小驼峰 | `studentName`、`pageResult` |
| 常量名 | 全大写，下划线分隔 | `MAX_PAGE_SIZE`、`CACHE_KEY_PREFIX` |
| 数据库表名 | 小写，下划线分隔，带模块前缀 | `sys_user`、`biz_student` |
| 数据库字段 | 小写，下划线分隔 | `create_time`、`student_name` |
| Mapper XML | 与 Mapper 接口同名 | `StudentMapper.xml` |

#### 后端方法命名约定

| 操作 | 前缀 | 示例 |
|------|------|------|
| 查询单个 | `get` / `select` | `getStudentById()` |
| 查询列表 | `list` / `selectList` | `listStudents()` |
| 分页查询 | `page` | `pageStudents()` |
| 新增 | `add` / `insert` / `save` | `addStudent()` |
| 修改 | `update` / `edit` | `updateStudent()` |
| 删除 | `delete` / `remove` | `deleteStudent()` |
| 统计 | `count` | `countStudent()` |
| 校验 | `check` | `checkStudentNameUnique()` |
| 导入 | `import` | `importStudent()` |
| 导出 | `export` | `exportStudent()` |

#### 前端命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 目录名 | 小写中划线 / 小驼峰 | `views/system/user/` |
| Vue 文件 | 大驼峰（组件）/ index.vue（页面） | `FileUpload.vue`、`index.vue` |
| JS 文件 | 小驼峰 | `request.js`、`student.js` |
| 组件名 | 大驼峰，多词 | `CrmTable`、`FileUpload` |
| 变量名 | 小驼峰 | `studentList`、`total` |
| 常量 | 全大写下划线 | `TOKEN_KEY` |
| 事件名 | 小驼峰，on 前缀 | `onSubmit`、`onReset` |
| CSS 类名 | 中划线分隔（BEM 可选） | `.student-list`、`.search-form` |

#### RESTful API 路径规范

| 操作 | Method | 路径 | 示例 |
|------|--------|------|------|
| 列表 | GET | `/{module}/list` | `GET /student/list` |
| 分页 | GET | `/{module}/page` | `GET /student/page` |
| 详情 | GET | `/{module}/{id}` | `GET /student/1` |
| 新增 | POST | `/{module}` | `POST /student` |
| 修改 | PUT | `/{module}` | `PUT /student` |
| 删除 | DELETE | `/{module}/{ids}` | `DELETE /student/1,2,3` |
| 状态修改 | PUT | `/{module}/status` | `PUT /student/status` |
| 导出 | GET | `/{module}/export` | `GET /student/export` |
| 导入 | POST | `/{module}/import` | `POST /student/import` |

### 1.7 开发流程总览

```
需求分析 → 数据库设计(ER图) → 代码生成器生成代码 → 完善业务逻辑
                                                      ↓
        部署上线 ← 论文撰写 ← 系统测试 ← 前后端联调 ← 页面开发
```

> 详细的开发流程见 [第七部分](#七系统开发流程)。

---

## 二、后台基础模块

> **设计目标**：以下 20+ 模块为所有系统通用的基础设施，一次开发，永久复用。业务系统开发时无需修改这些模块。

### 2.1 基础模块全景图

```
┌─────────────────────────────────────────────────────────────────┐
│                       后台基础模块全景                            │
├─────────────────┬─────────────────┬──────────────────────────────┤
│  认证与权限      │  系统管理        │  开发与运维                   │
│                 │                 │                              │
│  · 登录认证 JWT  │  · 用户管理      │  · 文件上传 / OSS            │
│  · RBAC 权限     │  · 角色管理      │  · 验证码                    │
│  · 数据权限      │  · 菜单管理      │  · 在线用户                  │
│  · XSS 防护      │  · 部门管理      │  · 缓存管理 (Redis)          │
│  · 防重复提交    │  · 岗位管理      │  · 任务调度 (Quartz)         │
│  · 接口限流      │  · 字典管理      │  · SQL 监控 (Druid)          │
│                 │  · 参数配置      │  · 接口文档 (Knife4j)        │
│                 │  · 通知公告      │  · 系统监控                  │
│                 │  · 操作日志      │  · 代码生成器                │
│                 │  · 登录日志      │                              │
└─────────────────┴─────────────────┴──────────────────────────────┘
```

### 2.2 认证与权限模块

#### 2.2.1 登录认证（JWT）

```
登录流程:
┌────────┐    ①账号密码+验证码    ┌──────────┐   ③校验成功
│ 浏览器  │ ──────────────────► │  Login   │ ──────────────┐
│        │                      │ Controller│               │
│        │ ◄────────────────── │           │   ④返回 Token  │
│        │   ② JWT Token        └──────────┘               │
│        │                                                   │
│        │    ⑤ 携带 Token 访问业务接口                        │
│        │ ──────────────────► ┌──────────┐                 │
│        │                    │ JwtAuth   │  ⑥解析Token      │
│        │                    │ Filter    │  ⑦校验Redis     │
│        │                    │           │  ⑧放行/拒绝     │
│        │ ◄────────────────── │           │                 │
│        │   ⑨ 业务数据 JSON    └──────────┘                 │
└────────┘                                                    │
```

**核心设计：**
- 登录成功后生成 JWT Token，有效期默认 30 分钟（可配置）
- Token 同时存入 Redis（key: `login_tokens:{uuid}`），支持主动登出和踢人
- 每次请求由 `JwtAuthFilter` 解析 Token，将用户信息存入 `SecurityContext`
- Token 快过期时自动续签（滑动过期）

#### 2.2.2 RBAC 权限模型

```
RBAC (Role-Based Access Control) 权限模型:

┌────────┐     ┌────────┐     ┌────────┐     ┌──────────┐
│  用户   │────►│  角色   │────►│  菜单   │────►│  权限标识 │
│ SysUser│ N:N │SysRole │ N:N │SysMenu │ 1:N │  perms   │
└────────┘     └────────┘     └────────┘     └──────────┘
                    │
                    ▼
              ┌──────────┐
              │  数据权限  │  ← 按部门隔离数据
              │ DataScope │
              └──────────┘

权限粒度:
├── 页面级: 菜单分配（控制侧边栏是否显示）
├── 接口级: @PreAuthorize("@ss.hasPermi('student:list')") 
└── 按钮级: 前端 v-hasPermi="['student:add']" 指令控制按钮显示/隐藏
```

**数据库表关系：**
- `sys_user_role`（用户-角色关联表，多对多）
- `sys_role_menu`（角色-菜单关联表，多对多）
- `sys_role_dept`（角色-部门关联表，用于数据权限）

#### 2.2.3 数据权限

通过 `@DataScope` 注解 + AOP 切面实现，按部门层级控制数据可见范围：

| 数据范围 | 含义 | 示例 |
|----------|------|------|
| `ALL` | 全部数据 | 管理员可看所有部门的数据 |
| `CUSTOM` | 自定义数据 | 可看指定部门的数据 |
| `DEPT` | 本部门 | 只看自己部门的数据 |
| `DEPT_AND_CHILD` | 本部门及子部门 | 看本部门及下属部门数据 |
| `SELF` | 仅本人 | 只看自己创建的数据 |

### 2.3 系统管理模块

#### 2.3.1 用户管理（SysUser）

| 功能 | 接口 | 说明 |
|------|------|------|
| 用户分页查询 | `GET /system/user/page` | 支持按部门、状态、时间筛选 |
| 新增用户 | `POST /system/user` | 含密码加密（BCrypt） |
| 修改用户 | `PUT /system/user` | 修改基本信息和角色 |
| 删除用户 | `DELETE /system/user/{ids}` | 逻辑删除 |
| 重置密码 | `PUT /system/user/resetPwd` | 管理员重置 |
| 状态切换 | `PUT /system/user/status` | 启用/停用 |
| 导入用户 | `POST /system/user/import` | Excel 批量导入 |
| 导出用户 | `GET /system/user/export` | Excel 导出 |

#### 2.3.2 角色管理（SysRole）

| 功能 | 说明 |
|------|------|
| 角色 CRUD | 角色 = 一组权限的集合 |
| 分配菜单 | 为角色绑定可见菜单和按钮权限 |
| 分配数据权限 | 为角色配置数据范围 |
| 分配用户 | 查看角色下的用户列表 |

#### 2.3.3 菜单管理（SysMenu）

菜单树形结构，支持三级类型：

| 菜单类型 | type 值 | 说明 |
|----------|---------|------|
| 目录 | `M` | 侧边栏一级目录（如"系统管理"） |
| 菜单 | `C` | 具体页面（如"用户管理"） |
| 按钮 | `F` | 页面内按钮权限（如"新增""删除"） |

菜单表核心字段：`menu_id`、`menu_name`、`parent_id`、`path`（路由）、`component`（组件路径）、`perms`（权限标识）、`icon`、`menu_type`、`order_num`

#### 2.3.4 部门管理（SysDept）

树形结构，用于数据权限隔离。核心字段：`dept_id`、`parent_id`、`ancestors`（祖级列表，逗号分隔的父 ID 链路，用于快速查询子部门）。

#### 2.3.5 岗位管理（SysPost）

用户与岗位为多对多关系，用于人员分类管理。

#### 2.3.6 字典管理（SysDict）

分两层：**字典类型（DictType）** + **字典数据（DictData）**。用于管理下拉框、单选框的选项。

```
字典类型: sys_user_sex (用户性别)
  ├── 字典数据: 0 → 男
  ├── 字典数据: 1 → 女
  └── 字典数据: 2 → 未知

字典类型: biz_student_type (学生类型)
  ├── 字典数据: 0 → 本科生
  ├── 字典数据: 1 → 硕士生
  └── 字典数据: 2 → 博士生
```

字典数据缓存在 Redis 中，前端通过 `useDict('sys_user_sex')` 组合式函数获取。

#### 2.3.7 参数配置（SysConfig）

管理系统运行参数，如：密码错误次数限制、Token 有效期、默认头像等。支持缓存，修改后自动刷新。

#### 2.3.8 通知公告（SysNotice）

站内通知发布与查看，支持富文本内容。

#### 2.3.9 操作日志（SysOperLog）

通过 `@Log(title = "学生管理", businessType = BusinessTypeEnum.INSERT)` 注解 + AOP 切面，自动记录操作人到日志表。记录内容：操作人、操作模块、操作类型、请求参数、返回结果、耗时、IP、异常信息。

#### 2.3.10 登录日志（SysLoginInfo）

记录所有登录行为：登录人、IP、地点、浏览器、操作系统、登录状态、消息、时间。

### 2.4 开发与运维模块

#### 2.4.1 文件上传 / OSS

统一文件上传接口，支持本地存储和云存储切换：

```
配置切换 (application.yml):
gp:
  file:
    type: local          # local | oss | minio
    local:
      path: /data/upload
      prefix: /static
    oss:
      endpoint: oss-cn-xxx.aliyuncs.com
      bucket: gp-bucket
```

上传接口返回统一的文件 URL，前端通过 `<FileUpload>` 或 `<ImageUpload>` 组件调用。

#### 2.4.2 验证码

使用图形验证码（算术运算 / 字符），验证码 Key 存入 Redis，登录时校验。

```
流程:
GET /captcha/image → 返回 { uuid, img(base64) }
POST /login { username, password, code, uuid } → 校验验证码 → 校验密码 → 返回 Token
```

#### 2.4.3 在线用户

从 Redis 中查询所有活跃 Token 对应的用户列表，支持强制登出（删除 Redis Token）。

#### 2.4.4 缓存管理（Redis）

- 查看 Redis 信息（内存使用、连接数、命中率）
- 查看所有缓存 Key
- 按 Key 前缀删除缓存
- 缓存分类：Token 缓存、字典缓存、参数缓存、验证码缓存、限流缓存

#### 2.4.5 任务调度（Quartz）

支持 Cron 表达式定时任务，可在线管理：

| 功能 | 说明 |
|------|------|
| 任务列表 | 查看所有定时任务 |
| 新增任务 | 配置 Cron 表达式和调用目标 |
| 启动/暂停 | 在线控制任务状态 |
| 执行一次 | 手动触发一次 |
| 任务日志 | 记录每次执行结果 |

#### 2.4.6 SQL 监控（Druid）

Druid 内置 SQL 监控面板（`/druid`），展示：SQL 执行次数、慢查询、连接池状态。

#### 2.4.7 接口文档（Knife4j）

Swagger 增强 UI，访问地址 `/doc.html`，自动生成接口文档，支持在线调试。

#### 2.4.8 系统监控

展示服务器信息：CPU、内存、JVM、磁盘、系统信息，通过 OSHI 库获取。

#### 2.4.9 代码生成器

读取数据库表结构，一键生成：

```
表结构 biz_student
       │
       ▼
  ┌──────────────────────────────────────┐
  │           代码生成器                   │
  ├──────────────────────────────────────┤
  │  生成后端:                             │
  │    Student.java (Entity)              │
  │    StudentDTO.java / StudentVO.java   │
  │    StudentMapper.java + XML           │
  │    StudentService + Impl              │
  │    StudentController                  │
  │    StudentMenu.sql (菜单SQL)           │
  │                                       │
  │  生成前端:                             │
  │    api/business/student.js            │
  │    views/business/student/index.vue   │
  └──────────────────────────────────────┘
```

### 2.5 公共基础设施

#### 2.5.1 统一返回结果（Result）

所有接口返回统一的 `Result<T>` 结构（详见 [第六部分 - Result 模板](#61-统一返回结果result)）。

#### 2.5.2 统一分页（PageQuery + PageResult）

所有分页接口统一使用 `PageQuery` 接收参数，返回 `PageResult<T>`。

#### 2.5.3 统一异常处理

`GlobalExceptionHandler` 捕获所有异常，统一转为 `Result` 返回（详见 [第六部分 - 异常模板](#615-统一异常模板)）。

#### 2.5.4 统一日志

Logback 配置，按天滚动日志文件，分 `info`、`error` 两个级别存储。

---

## 三、业务模块规范

> **核心理念**：所有业务模块遵循统一的"十五件套"规范。开发一个新业务模块 = 复制模板 + 修改实体名。

### 3.1 业务模块文件清单（十五件套）

以 `Student`（学生管理）为例，一个完整的业务模块包含以下 **15 个文件**：

```
business/student/
├── domain/
│   ├── Student.java               ① 实体类（对应数据库表）
│   ├── StudentDTO.java            ② 数据传输对象（新增/修改入参）
│   ├── StudentVO.java             ③ 视图对象（详情/列表出参）
│   └── StudentQuery.java          ④ 查询条件对象（分页+筛选）
├── mapper/
│   ├── StudentMapper.java         ⑤ Mapper 接口
│   └── xml/StudentMapper.xml      ⑥ Mapper XML（复杂SQL）
├── service/
│   ├── StudentService.java        ⑦ Service 接口
│   └── impl/
│       └── StudentServiceImpl.java⑧ Service 实现类
├── controller/
│   └── StudentController.java     ⑨ Controller 控制器
├── convert/
│   └── StudentConvert.java        ⑩ 对象转换器（MapStruct）
├── excel/
│   └── StudentExcel.java          ⑪ Excel 导入导出对象
├── enums/
│   └── StudentStatusEnum.java     ⑫ 业务枚举（如状态枚举）
└── constant/
    └── StudentConstant.java       ⑬ 模块常量

前端 2 个文件:
├── api/business/student.js        ⑭ API 请求封装
└── views/business/student/index.vue⑮ 页面视图
```

### 3.2 各层职责定义

| 层 | 文件 | 职责 | 禁止事项 |
|----|------|------|----------|
| Controller | `StudentController` | 接收请求、参数校验、调用 Service、返回 Result | ❌ 不写业务逻辑 |
| Service | `StudentService` | 定义业务接口 | ❌ 不写实现 |
| ServiceImpl | `StudentServiceImpl` | 业务逻辑实现、事务控制 | ❌ 不直接操作数据库 |
| Mapper | `StudentMapper` | 数据访问、SQL | ❌ 不写业务逻辑 |
| Entity | `Student` | 数据库映射 | ❌ 不加业务方法 |
| DTO | `StudentDTO` | 接收前端入参 | ❌ 不含敏感字段 |
| VO | `StudentVO` | 返回前端数据 | ❌ 不直接暴露 Entity |
| Query | `StudentQuery` | 查询条件封装 | ❌ 不含无关字段 |
| Convert | `StudentConvert` | 对象转换 | ❌ 手动 getter/setter |

### 3.3 标准 CRUD 接口清单

每个业务模块 **必须** 提供以下接口：

| 序号 | 功能 | Method | 路径 | 入参 | 出参 |
|------|------|--------|------|------|------|
| 1 | 分页查询 | GET | `/student/page` | StudentQuery | PageResult\<StudentVO> |
| 2 | 列表查询 | GET | `/student/list` | StudentQuery | List\<StudentVO> |
| 3 | 详情查询 | GET | `/student/{id}` | id | StudentVO |
| 4 | 新增 | POST | `/student` | StudentDTO | Result |
| 5 | 修改 | PUT | `/student` | StudentDTO | Result |
| 6 | 删除 | DELETE | `/student/{ids}` | ids | Result |
| 7 | 批量删除 | DELETE | `/student/batch/{ids}` | ids | Result |
| 8 | 状态修改 | PUT | `/student/status` | id, status | Result |
| 9 | 导出 | GET | `/student/export` | StudentQuery | Excel 文件 |
| 10 | 导入 | POST | `/student/import` | MultipartFile | Result |
| 11 | 模板下载 | GET | `/student/importTemplate` | - | Excel 文件 |

### 3.4 实体对象流转图

```
                    前端
                     │
        ┌────────────┼────────────┐
        ▼            ▼            ▼
   StudentDTO   StudentQuery   Form表单
   (新增/修改)   (查询条件)       
        │            │
        ▼            ▼
   StudentConvert    │
   (DTO → Entity)    │
        │            │
        ▼            ▼
  ┌─────────────────────┐
  │  StudentServiceImpl  │ ← 业务逻辑
  └──────────┬──────────┘
             │
             ▼
      StudentMapper
      (Entity → DB)
             │
             ▼
         MySQL
             │
             ▼
      StudentMapper
      (DB → Entity)
             │
             ▼
   StudentConvert
   (Entity → VO)
             │
             ▼
      StudentVO ──────► Result<PageResult<StudentVO>> ──► 前端
```

**核心原则：Entity 永远不直接暴露给前端，必须经过 DTO（入）或 VO（出）转换。**

### 3.5 业务模块开发检查清单

开发一个新业务模块时，按以下清单逐项完成：

- [ ] ① 设计数据库表（遵循 [第五部分](#五数据库模板) 规范）
- [ ] ② 编写实体类 `Student.java`（继承 `BaseEntity`）
- [ ] ③ 编写 DTO / VO / Query 对象
- [ ] ④ 编写 Convert 转换器（MapStruct）
- [ ] ⑤ 编写 Mapper 接口 + XML
- [ ] ⑥ 编写 Service 接口 + 实现类
- [ ] ⑦ 编写 Controller（包含全部 CRUD 接口）
- [ ] ⑧ 编写 Excel 导入导出对象
- [ ] ⑨ 编写业务枚举
- [ ] ⑩ 配置菜单 SQL（插入菜单和按钮权限）
- [ ] ⑪ 编写前端 API 封装
- [ ] ⑫ 编写前端页面（列表 + 表单弹窗 + 详情）
- [ ] ⑬ 测试全部接口（Postman / Knife4j）
- [ ] ⑭ 前后端联调

---

## 四、页面模板（Vue3 + Element Plus）

> **设计目标**：所有业务页面共享一套统一的页面模板，开发新页面 = 复制模板 + 修改字段。所有模板基于 Vue3 `<script setup>` + Element Plus。

### 4.1 页面模板体系

```
┌─────────────────────────────────────────────────────────┐
│                    业务页面统一模板                       │
├─────────────────┬───────────────────────────────────────┤
│  列表页(标准)    │  搜索栏 + 表格 + 分页 + 工具栏(增删导出)│
│  树形列表页      │  左侧树 + 右侧表格（如部门管理）        │
│  表单弹窗        │  新增/编辑弹窗（表单校验）              │
│  详情页          │  只读展示，描述列表                    │
│  统计页          │  ECharts 图表 + 数据卡片               │
│  标签页          │  多 Tab 切换                          │
├─────────────────┴───────────────────────────────────────┤
│                    公共组件                              │
│  CrmTable / CrmSearch / CrmDialog / FileUpload          │
│  ImageUpload / RichEditor / TreeSelect / DictTag        │
└─────────────────────────────────────────────────────────┘
```

### 4.2 列表页标准模板（核心模板 ★）

这是所有业务页面使用频率最高的模板。以"学生管理"为例：

```vue
<template>
  <div class="app-container">
    <!-- ========== 搜索栏 ========== -->
    <el-form :model="queryParams" ref="queryRef" :inline="true" v-show="showSearch">
      <el-form-item label="学生姓名" prop="studentName">
        <el-input v-model="queryParams.studentName" placeholder="请输入" clearable
          @keyup.enter="handleQuery" style="width: 200px" />
      </el-form-item>
      <el-form-item label="学号" prop="studentNo">
        <el-input v-model="queryParams.studentNo" placeholder="请输入" clearable
          style="width: 200px" />
      </el-form-item>
      <el-form-item label="状态" prop="status">
        <el-select v-model="queryParams.status" placeholder="请选择" clearable style="width: 200px">
          <el-option v-for="dict in sys_normal_disable"
            :key="dict.value" :label="dict.label" :value="dict.value" />
        </el-select>
      </el-form-item>
      <el-form-item label="创建时间" style="width: 388px">
        <el-date-picker v-model="dateRange" value-format="YYYY-MM-DD"
          type="daterange" range-separator="-" start-placeholder="开始" end-placeholder="结束" />
      </el-form-item>
      <el-form-item>
        <el-button type="primary" icon="Search" @click="handleQuery">搜索</el-button>
        <el-button icon="Refresh" @click="resetQuery">重置</el-button>
      </el-form-item>
    </el-form>

    <!-- ========== 操作工具栏 ========== -->
    <el-row :gutter="10" class="mb8">
      <el-col :span="1.5">
        <el-button type="primary" plain icon="Plus" @click="handleAdd"
          v-hasPermi="['student:add']">新增</el-button>
      </el-col>
      <el-col :span="1.5">
        <el-button type="success" plain icon="Edit" :disabled="single" @click="handleUpdate"
          v-hasPermi="['student:edit']">修改</el-button>
      </el-col>
      <el-col :span="1.5">
        <el-button type="danger" plain icon="Delete" :disabled="multiple" @click="handleDelete"
          v-hasPermi="['student:delete']">删除</el-button>
      </el-col>
      <el-col :span="1.5">
        <el-button type="warning" plain icon="Download" @click="handleExport"
          v-hasPermi="['student:export']">导出</el-button>
      </el-col>
      <right-toolbar v-model:showSearch="showSearch" @queryTable="getList"></right-toolbar>
    </el-row>

    <!-- ========== 数据表格 ========== -->
    <el-table v-loading="loading" :data="tableData" @selection-change="handleSelectionChange">
      <el-table-column type="selection" width="55" align="center" />
      <el-table-column label="序号" type="index" width="55" align="center" />
      <el-table-column label="学号" prop="studentNo" />
      <el-table-column label="学生姓名" prop="studentName" :show-overflow-tooltip="true" />
      <el-table-column label="性别" prop="gender" align="center">
        <template #default="scope">
          <dict-tag :options="sys_user_sex" :value="scope.row.gender" />
        </template>
      </el-table-column>
      <el-table-column label="所属班级" prop="className" />
      <el-table-column label="状态" prop="status" align="center">
        <template #default="scope">
          <el-switch v-model="scope.row.status" active-value="0" inactive-value="1"
            @change="handleStatusChange(scope.row)" />
        </template>
      </el-table-column>
      <el-table-column label="创建时间" prop="createTime" width="160" />
      <el-table-column label="操作" width="180" align="center" fixed="right">
        <template #default="scope">
          <el-button link type="primary" icon="Edit" @click="handleUpdate(scope.row)"
            v-hasPermi="['student:edit']">修改</el-button>
          <el-button link type="primary" icon="Delete" @click="handleDelete(scope.row)"
            v-hasPermi="['student:delete']">删除</el-button>
        </template>
      </el-table-column>
    </el-table>

    <!-- ========== 分页 ========== -->
    <el-pagination v-show="total > 0" :total="total" v-model:current-page="queryParams.pageNum"
      v-model:page-size="queryParams.pageSize" :page-sizes="[10, 20, 50, 100]"
      layout="total, sizes, prev, pager, next, jumper" class="pagination" @size-change="getList"
      @current-change="getList" />

    <!-- ========== 新增/修改弹窗 ========== -->
    <el-dialog :title="title" v-model="dialogVisible" width="600px" append-to-body>
      <el-form ref="formRef" :model="form" :rules="rules" label-width="90px">
        <el-row :gutter="20">
          <el-col :span="12">
            <el-form-item label="学号" prop="studentNo">
              <el-input v-model="form.studentNo" placeholder="请输入学号" />
            </el-form-item>
          </el-col>
          <el-col :span="12">
            <el-form-item label="学生姓名" prop="studentName">
              <el-input v-model="form.studentName" placeholder="请输入姓名" />
            </el-form-item>
          </el-col>
          <el-col :span="12">
            <el-form-item label="性别" prop="gender">
              <el-select v-model="form.gender" placeholder="请选择">
                <el-option v-for="dict in sys_user_sex"
                  :key="dict.value" :label="dict.label" :value="dict.value" />
              </el-select>
            </el-form-item>
          </el-col>
          <el-col :span="12">
            <el-form-item label="所属班级" prop="className">
              <el-input v-model="form.className" placeholder="请输入班级" />
            </el-form-item>
          </el-col>
          <el-col :span="24">
            <el-form-item label="头像" prop="avatar">
              <image-upload v-model="form.avatar" :limit="1" />
            </el-form-item>
          </el-col>
        </el-row>
      </el-form>
      <template #footer>
        <el-button type="primary" @click="submitForm">确 定</el-button>
        <el-button @click="cancel">取 消</el-button>
      </template>
    </el-dialog>
  </div>
</template>

<script setup>
import { ref, reactive, onMounted } from 'vue'
import { pageStudent, getStudent, addStudent, updateStudent, delStudent, changeStudentStatus } from '@/api/business/student'

const { proxy } = getCurrentInstance()
// 字典组合式函数（框架内置）
const { sys_normal_disable, sys_user_sex } = useDict('sys_normal_disable', 'sys_user_sex')

// ===== 响应式状态 =====
const loading = ref(false)
const tableData = ref([])
const total = ref(0)
const showSearch = ref(true)
const single = ref(true)      // 非单个禁用
const multiple = ref(true)    // 非多个禁用
const ids = ref([])           // 选中的ID数组
const dialogVisible = ref(false)
const title = ref('')
const dateRange = ref([])

// 查询参数
const queryParams = reactive({
  pageNum: 1, pageSize: 10,
  studentName: undefined, studentNo: undefined, status: undefined
})

// 表单数据
const form = ref({})
const rules = reactive({
  studentNo: [{ required: true, message: '学号不能为空', trigger: 'blur' }],
  studentName: [{ required: true, message: '姓名不能为空', trigger: 'blur' }]
})

// ===== 生命周期 =====
onMounted(() => getList())

// ===== 方法 =====
/** 查询列表 */
function getList() {
  loading.value = true
  pageStudent(proxy.addDateRange(queryParams, dateRange)).then(res => {
    tableData.value = res.data.rows
    total.value = res.data.total
  }).finally(() => loading.value = false)
}

/** 搜索 */
function handleQuery() { queryParams.pageNum = 1; getList() }

/** 重置 */
function resetQuery() {
  dateRange.value = []
  proxy.resetForm('queryRef')
  handleQuery()
}

/** 多选 */
function handleSelectionChange(selection) {
  ids.value = selection.map(item => item.id)
  single.value = selection.length !== 1
  multiple.value = !selection.length
}

/** 新增 */
function handleAdd() {
  reset(); title.value = '新增学生'; dialogVisible.value = true
}

/** 修改 */
function handleUpdate(row) {
  const id = row?.id || ids.value[0]
  reset()
  getStudent(id).then(res => { form.value = res.data; title.value = '修改学生'; dialogVisible.value = true })
}

/** 提交 */
function submitForm() {
  proxy.$refs['formRef'].validate(valid => {
    if (!valid) return
    if (form.value.id != null) {
      updateStudent(form.value).then(() => { proxy.$modal.msgSuccess('修改成功'); dialogVisible.value = false; getList() })
    } else {
      addStudent(form.value).then(() => { proxy.$modal.msgSuccess('新增成功'); dialogVisible.value = false; getList() })
    }
  })
}

/** 删除 */
function handleDelete(row) {
  const deleteIds = row?.id || ids.value
  proxy.$modal.confirm('确认删除选中的数据？').then(() => {
    return delStudent(deleteIds)
  }).then(() => { getList(); proxy.$modal.msgSuccess('删除成功') }).catch(() => {})
}

/** 状态切换 */
function handleStatusChange(row) {
  const text = row.status === '0' ? '启用' : '停用'
  proxy.$modal.confirm('确认' + text + '该学生？').then(() => {
    return changeStudentStatus(row.id, row.status)
  }).then(() => proxy.$modal.msgSuccess(text + '成功')).catch(() => {
    row.status = row.status === '0' ? '1' : '0'
  })
}

/** 导出 */
function handleExport() {
  proxy.download('student/export', { ...queryParams }, `student_${new Date().getTime()}.xlsx`)
}

/** 表单重置 */
function reset() {
  form.value = { studentNo: undefined, studentName: undefined, gender: '0', className: undefined, avatar: undefined }
  proxy.resetForm('formRef')
}

/** 取消 */
function cancel() { dialogVisible.value = false; reset() }
</script>

<style scoped lang="scss">
.pagination { float: right; margin-top: 15px; }
.mb8 { margin-bottom: 8px; }
</style>
```

> **使用方式**：复制此模板 → 将 `Student` / `student` 替换为新业务名 → 修改字段名和校验规则 → 页面开发完成。

### 4.3 高级搜索栏模板

搜索栏支持展开/收起（`v-show="showSearch"`），通过 `<right-toolbar>` 组件的右上角搜索图标控制。

支持的搜索控件：
- 文本输入框 `el-input`
- 下拉选择 `el-select`（配合字典 `useDict`）
- 日期范围 `el-date-picker type="daterange"`
- 数字范围
- 级联选择 `el-cascader`
- 树形选择 `el-tree-select`

### 4.4 表单弹窗模板

表单弹窗的核心规范：
- 使用 `el-dialog` + `el-form` 组合
- 表单校验通过 `rules` 配置（必填、邮箱格式、手机号格式等）
- 布局采用 `el-row` + `el-col` 栅格布局
- 新增和修改共用一个弹窗，通过 `form.id` 区分

常用表单控件：

| 类型 | 组件 | 用途 |
|------|------|------|
| 文本 | `el-input` | 姓名、编号 |
| 下拉 | `el-select` | 状态、类型（配合字典） |
| 日期 | `el-date-picker` | 入学时间、出生日期 |
| 数字 | `el-input-number` | 年龄、数量 |
| 单选 | `el-radio-group` | 性别 |
| 多选 | `el-checkbox-group` | 标签 |
| 开关 | `el-switch` | 启用/停用 |
| 文本域 | `el-input type="textarea"` | 备注、描述 |
| 图片上传 | `<image-upload>` | 头像、封面 |
| 文件上传 | `<file-upload>` | 附件 |
| 富文本 | `<rich-editor>` | 详情、内容 |
| 级联 | `el-cascader` | 省市区、部门 |

### 4.5 公共组件库

#### 4.5.1 文件上传（FileUpload）

```vue
<!-- 用法：v-model 绑定文件URL，支持多文件 -->
<file-upload v-model="form.fileUrl" :limit="5" :fileType="['doc', 'docx', 'pdf']" />
```

特性：支持拖拽上传、文件类型限制、文件大小限制、进度条、预览。

#### 4.5.2 图片上传（ImageUpload）

```vue
<!-- 用法：v-model 绑定图片URL，支持多图 -->
<image-upload v-model="form.avatar" :limit="1" :fileSize="2" />
```

特性：支持拖拽、裁剪、预览、类型限制（jpg/png）。

#### 4.5.3 富文本编辑器（RichEditor）

```vue
<!-- 用法：基于 wangEditor 5 封装 -->
<rich-editor v-model="form.content" :height="300" />
```

#### 4.5.4 树形选择（TreeSelect）

```vue
<!-- 用法：部门树选择 -->
<tree-select v-model="form.deptId" :options="deptOptions" :objMap="{value:'id', label:'name'}" />
```

#### 4.5.5 字典标签（DictTag）

```vue
<!-- 用法：根据字典值显示对应的标签和颜色 -->
<dict-tag :options="sys_user_sex" :value="scope.row.gender" />
```

### 4.6 首页 Dashboard 模板

首页通常包含：
- 数据统计卡片（总用户数、今日订单、活跃用户、收入）
- ECharts 柱状图 / 折线图 / 饼图
- 快捷操作入口
- 系统通知公告

```vue
<!-- 统计卡片 -->
<el-row :gutter="20">
  <el-col :span="6" v-for="card in statCards" :key="card.title">
    <el-card shadow="hover">
      <div class="stat-card">
        <el-icon class="stat-icon" :style="{ background: card.color }"><component :is="card.icon" /></el-icon>
        <div class="stat-info">
          <div class="stat-value">{{ card.value }}</div>
          <div class="stat-title">{{ card.title }}</div>
        </div>
      </div>
    </el-card>
  </el-col>
</el-row>

<!-- ECharts 图表 -->
<el-row :gutter="20" style="margin-top:20px">
  <el-col :span="16"><el-card><div ref="barChartRef" style="height:350px"></div></el-card></el-col>
  <el-col :span="8"><el-card><div ref="pieChartRef" style="height:350px"></div></el-card></el-col>
</el-row>
```

### 4.7 自定义指令

| 指令 | 用法 | 说明 |
|------|------|------|
| `v-hasPermi` | `v-hasPermi="['student:add']"` | 按钮级权限控制，无权限自动隐藏 |
| `v-hasRole` | `v-hasRole="['admin']"` | 角色级权限控制 |
| `v-dialogDrag` | `<el-dialog v-dialogDrag>` | 弹窗可拖拽 |

### 4.8 通用弹窗工具（$modal）

框架封装的全局弹窗方法，通过 `proxy.$modal` 调用：

```javascript
// 消息提示
proxy.$modal.msgSuccess('操作成功')
proxy.$modal.msgError('操作失败')
proxy.$modal.msgWarning('警告信息')

// 确认弹窗（返回 Promise）
proxy.$modal.confirm('确认删除？').then(() => { /* 确认 */ })

// 加载遮罩
proxy.$modal.loading('加载中...')
proxy.$modal.closeLoading()

// 下载
proxy.download('student/export', params, '文件名.xlsx')
```

---

## 五、数据库模板

> **设计目标**：统一的数据库设计规范，确保所有表结构一致、可追溯、易维护。

### 5.1 数据库通用字段规范

**所有业务表必须包含以下审计字段**（通过 `BaseEntity` 统一管理）：

| 字段名 | 类型 | 说明 | 是否必填 |
|--------|------|------|----------|
| `id` | `BIGINT` | 主键，自增 | ★ 是 |
| `remark` | `VARCHAR(500)` | 备注 | 否 |
| `create_by` | `VARCHAR(64)` | 创建人 | ★ 自动填充 |
| `create_time` | `DATETIME` | 创建时间 | ★ 自动填充 |
| `update_by` | `VARCHAR(64)` | 更新人 | ★ 自动填充 |
| `update_time` | `DATETIME` | 更新时间 | ★ 自动填充 |
| `del_flag` | `CHAR(1)` | 逻辑删除标志（0 存在 2 删除） | ★ 默认 0 |

**业务表建议包含以下可选字段**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `status` | `CHAR(1)` | 状态（0 启用 1 停用） |
| `order_num` | `INT` | 排序号 |
| `dept_id` | `BIGINT` | 部门 ID（用于数据权限） |
| `tenant_id` | `VARCHAR(20)` | 租户 ID（如需多租户） |

### 5.2 数据库字段命名规范

| 规则 | 正确示例 | 错误示例 |
|------|----------|----------|
| 小写 + 下划线 | `student_name` | `studentName`、`StudentName` |
| 布尔值用 `is_` 前缀 | `is_deleted` | `deleted` |
| 时间字段用 `_time` 或 `_date` 后缀 | `create_time` | `create` |
| 金额字段用 `Fen`/`Cent` 后缀 | `total_amount` | `money` |
| 关联字段用 `_id` 后缀 | `dept_id` | `dept` |
| 状态字段用 `_status` 或 `_flag` 后缀 | `del_flag` | `del` |
| 命名见名知义 | `email` | `e`、`mail_address` |

### 5.3 数据库字段类型规范

| 数据类型 | 推荐类型 | 说明 |
|----------|----------|------|
| 主键 ID | `BIGINT` | 自增或雪花算法，禁用 UUID |
| 短文本 | `VARCHAR(64)` ~ `VARCHAR(255)` | 姓名、标题 |
| 长文本 | `TEXT` / `LONGTEXT` | 内容、描述 |
| 整数 | `INT` / `BIGINT` | 数量、计数 |
| 金额 | `DECIMAL(10,2)` | 禁用 FLOAT/DOUBLE |
| 日期时间 | `DATETIME` | 精确到秒 |
| 日期 | `DATE` | 仅日期 |
| 布尔/状态 | `CHAR(1)` | '0'/'1' |
| 枚举值 | `CHAR(1)` 或 `TINYINT` | |

### 5.4 标准建表 SQL 模板

以 `biz_student`（学生表）为例：

```sql
-- ========================================
-- 业务表：学生管理 biz_student
-- 模块：business.student
-- ========================================
DROP TABLE IF EXISTS `biz_student`;
CREATE TABLE `biz_student` (
  `id`            BIGINT(20)    NOT NULL AUTO_INCREMENT  COMMENT '主键ID',
  `student_no`    VARCHAR(32)   NOT NULL                 COMMENT '学号',
  `student_name`  VARCHAR(64)   NOT NULL                 COMMENT '学生姓名',
  `gender`        CHAR(1)       DEFAULT '0'              COMMENT '性别（0男 1女 2未知）',
  `birth_date`    DATE          DEFAULT NULL             COMMENT '出生日期',
  `phone`         VARCHAR(20)   DEFAULT NULL             COMMENT '联系电话',
  `email`         VARCHAR(128)  DEFAULT NULL             COMMENT '邮箱',
  `class_id`      BIGINT(20)    DEFAULT NULL             COMMENT '班级ID',
  `class_name`    VARCHAR(64)   DEFAULT NULL             COMMENT '班级名称（冗余）',
  `enroll_date`   DATE          DEFAULT NULL             COMMENT '入学日期',
  `avatar`        VARCHAR(255)  DEFAULT NULL             COMMENT '头像URL',
  `address`       VARCHAR(255)  DEFAULT NULL             COMMENT '家庭住址',
  `status`        CHAR(1)       DEFAULT '0'              COMMENT '状态（0启用 1停用）',
  `dept_id`       BIGINT(20)    DEFAULT NULL             COMMENT '院系ID（数据权限）',
  `remark`        VARCHAR(500)  DEFAULT NULL             COMMENT '备注',
  `del_flag`      CHAR(1)       DEFAULT '0'              COMMENT '删除标志（0存在 2删除）',
  `create_by`     VARCHAR(64)   DEFAULT ''               COMMENT '创建者',
  `create_time`   DATETIME      DEFAULT NULL             COMMENT '创建时间',
  `update_by`     VARCHAR(64)   DEFAULT ''               COMMENT '更新者',
  `update_time`   DATETIME      DEFAULT NULL             COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_student_no` (`student_no`),
  KEY `idx_class_id` (`class_id`),
  KEY `idx_dept_id` (`dept_id`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='学生信息表';
```

### 5.5 索引规范

| 索引类型 | 命名规范 | 示例 | 使用场景 |
|----------|----------|------|----------|
| 主键 | `PRIMARY KEY` | `PRIMARY KEY (id)` | 自增ID |
| 唯一索引 | `uk_字段名` | `uk_student_no` | 学号、手机号（不重复字段） |
| 普通索引 | `idx_字段名` | `idx_class_id` | 外键字段、常用查询字段 |
| 联合索引 | `idx_字段1_字段2` | `idx_dept_status` | 多字段组合查询 |
| 前缀索引 | `idx_字段名(前缀长度)` | `idx_content(50)` | 长文本前缀索引 |

**索引使用原则：**
- 每张表必须有主键
- 唯一业务字段必须加唯一索引（如学号）
- 外键关联字段必须加普通索引
- 常用查询条件字段考虑加索引
- 单表索引数量不超过 5 个
- WHERE、ORDER BY、GROUP BY 中频繁使用的字段加索引
- 避免在索引列上使用函数

### 5.6 外键规范

**毕设项目推荐：不使用物理外键，通过应用层维护关联关系。**

原因：
- 物理外键影响性能
- 不利于分库分表
- 逻辑删除时外键约束会冲突
- 降低数据导入导出灵活性

替代方案：通过代码校验关联完整性，数据库层仅通过索引加速关联查询。

### 5.7 SQL 编写规范

**SELECT 规范：**
```sql
-- ✅ 正确：明确指定字段
SELECT id, student_name, gender, class_name FROM biz_student WHERE del_flag = '0';

-- ❌ 错误：禁用 SELECT *
SELECT * FROM biz_student;
```

**WHERE 规范：**
```sql
-- ✅ 所有查询必须带逻辑删除条件
WHERE del_flag = '0' AND status = '0'

-- ✅ 避免全表扫描
WHERE student_name LIKE '张%'    -- 走索引
-- ❌ 前置百分号不走索引
WHERE student_name LIKE '%张%'   -- 全表扫描
```

**分页规范：**
```sql
-- ✅ 使用 LIMIT 分页
SELECT id, student_name FROM biz_student
WHERE del_flag = '0'
ORDER BY id DESC
LIMIT 10 OFFSET 0;
```

**批量操作规范：**
```sql
-- ✅ 批量插入
INSERT INTO biz_student (student_no, student_name) VALUES
('2024001', '张三'),
('2024002', '李四'),
('2024003', '王五');
```

### 5.8 数据库初始化 SQL 脚本

所有表和初始化数据通过 SQL 脚本管理，存放在 `sql/` 目录：

```
sql/
├── 01_gp_framework.sql       # 框架基础表（系统模块 15+ 张表）
├── 02_gp_init_data.sql       # 框架初始化数据（管理员、默认菜单等）
├── 03_gp_business.sql        # 业务模块建表 SQL
└── 04_gp_business_menu.sql   # 业务模块菜单权限 SQL
```

**框架基础表清单（15 张）：**

| 表名 | 说明 |
|------|------|
| `sys_user` | 用户表 |
| `sys_role` | 角色表 |
| `sys_menu` | 菜单权限表 |
| `sys_dept` | 部门表 |
| `sys_post` | 岗位表 |
| `sys_user_role` | 用户-角色关联表 |
| `sys_role_menu` | 角色-菜单关联表 |
| `sys_role_dept` | 角色-部门关联表（数据权限） |
| `sys_user_post` | 用户-岗位关联表 |
| `sys_dict_type` | 字典类型表 |
| `sys_dict_data` | 字典数据表 |
| `sys_config` | 参数配置表 |
| `sys_notice` | 通知公告表 |
| `sys_oper_log` | 操作日志表 |
| `sys_login_info` | 登录日志表 |

---

## 六、代码模板

> **使用方式**：以下所有模板以 `Student`（学生管理）为示例实体，开发新模块时全局替换 `Student` / `student` / `biz_student` 即可。

### 6.1 统一返回结果（Result）

```java
package com.gp.common.core.result;

import com.gp.common.enums.ResultCodeEnum;
import lombok.Data;
import java.io.Serializable;

/**
 * 统一返回结果
 * 所有 Controller 接口必须返回此对象
 */
@Data
public class Result<T> implements Serializable {

    private Integer code;   // 状态码：200=成功，500=失败
    private String msg;     // 返回消息
    private T data;         // 返回数据

    // ===== 成功 =====
    public static <T> Result<T> success() {
        return result(null, ResultCodeEnum.SUCCESS.getCode(), "操作成功");
    }

    public static <T> Result<T> success(T data) {
        return result(data, ResultCodeEnum.SUCCESS.getCode(), "操作成功");
    }

    public static <T> Result<T> success(T data, String msg) {
        return result(data, ResultCodeEnum.SUCCESS.getCode(), msg);
    }

    // ===== 失败 =====
    public static <T> Result<T> error() {
        return result(null, ResultCodeEnum.ERROR.getCode(), "操作失败");
    }

    public static <T> Result<T> error(String msg) {
        return result(null, ResultCodeEnum.ERROR.getCode(), msg);
    }

    public static <T> Result<T> error(Integer code, String msg) {
        return result(null, code, msg);
    }

    // ===== 构建通用方法 =====
    private static <T> Result<T> result(T data, Integer code, String msg) {
        Result<T> r = new Result<>();
        r.setData(data);
        r.setCode(code);
        r.setMsg(msg);
        return r;
    }
}
```

### 6.2 统一分页（PageQuery + PageResult）

```java
package com.gp.common.core.page;

import lombok.Data;

/**
 * 分页查询基类
 * 所有业务查询对象 Query 继承此类
 */
@Data
public class PageQuery {
    /** 当前页码 */
    private Integer pageNum = 1;

    /** 每页条数 */
    private Integer pageSize = 10;

    /** 排序字段 */
    private String orderByColumn;

    /** 排序方向：asc / desc */
    private String isAsc;
}
```

```java
package com.gp.common.core.page;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import java.util.List;

/**
 * 分页结果封装
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class PageResult<T> {
    /** 总记录数 */
    private Long total;

    /** 当前页数据 */
    private List<T> rows;

    /** 当前页码 */
    private Integer pageNum;

    /** 每页条数 */
    private Integer pageSize;

    public PageResult(Long total, List<T> rows) {
        this.total = total;
        this.rows = rows;
    }
}
```

### 6.3 实体基类（BaseEntity）

```java
package com.gp.common.core.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;
import java.io.Serializable;
import java.util.Date;

/**
 * 实体基类
 * 所有实体类（Entity）继承此类，自动包含审计字段
 */
@Data
public class BaseEntity implements Serializable {

    /** 主键ID */
    @TableId(type = IdType.AUTO)
    private Long id;

    /** 备注 */
    private String remark;

    /** 创建者 */
    @TableField(fill = FieldFill.INSERT)
    private String createBy;

    /** 创建时间 */
    @TableField(fill = FieldFill.INSERT)
    private Date createTime;

    /** 更新者 */
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private String updateBy;

    /** 更新时间 */
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private Date updateTime;

    /** 删除标志（0存在 2删除） */
    @TableLogic
    @TableField(fill = FieldFill.INSERT)
    private String delFlag;
}
```

### 6.4 自动填充处理器

```java
package com.gp.common.handler;

import com.baomidou.mybatisplus.core.handlers.MetaObjectHandler;
import com.gp.common.utils.SecurityUtils;
import org.apache.ibatis.reflection.MetaObject;
import org.springframework.stereotype.Component;
import java.util.Date;

/**
 * MyBatis Plus 自动填充处理器
 * 自动填充 createBy / createTime / updateBy / updateTime / delFlag
 */
@Component
public class MyMetaObjectHandler implements MetaObjectHandler {

    @Override
    public void insertFill(MetaObject metaObject) {
        this.strictInsertFill(metaObject, "createTime", Date.class, new Date());
        this.strictInsertFill(metaObject, "updateTime", Date.class, new Date());
        this.strictInsertFill(metaObject, "createBy", String.class, getCurrentUsername());
        this.strictInsertFill(metaObject, "updateBy", String.class, getCurrentUsername());
        this.strictInsertFill(metaObject, "delFlag", String.class, "0");
    }

    @Override
    public void updateFill(MetaObject metaObject) {
        this.strictUpdateFill(metaObject, "updateTime", Date.class, new Date());
        this.strictUpdateFill(metaObject, "updateBy", String.class, getCurrentUsername());
    }

    private String getCurrentUsername() {
        try {
            return SecurityUtils.getUsername();
        } catch (Exception e) {
            return "system";
        }
    }
}
```

### 6.5 Entity 模板（Student）

```java
package com.gp.business.student.domain;

import com.baomidou.mybatisplus.annotation.TableName;
import com.gp.common.annotation.Excel;
import com.gp.common.core.entity.BaseEntity;
import lombok.Data;
import lombok.EqualsAndHashCode;
import java.util.Date;

/**
 * 学生信息实体
 * 对应数据库表: biz_student
 */
@Data
@EqualsAndHashCode(callSuper = true)
@TableName("biz_student")
public class Student extends BaseEntity {

    /** 学号 */
    @Excel(name = "学号")
    private String studentNo;

    /** 学生姓名 */
    @Excel(name = "学生姓名")
    private String studentName;

    /** 性别（0男 1女 2未知） */
    @Excel(name = "性别", readConverterExp = "0=男,1=女,2=未知")
    private String gender;

    /** 出生日期 */
    private Date birthDate;

    /** 联系电话 */
    @Excel(name = "联系电话")
    private String phone;

    /** 邮箱 */
    private String email;

    /** 班级ID */
    private Long classId;

    /** 班级名称 */
    @Excel(name = "班级")
    private String className;

    /** 入学日期 */
    private Date enrollDate;

    /** 头像URL */
    private String avatar;

    /** 家庭住址 */
    private String address;

    /** 状态（0启用 1停用） */
    @Excel(name = "状态", readConverterExp = "0=启用,1=停用")
    private String status;

    /** 院系ID（数据权限） */
    private Long deptId;
}
```

### 6.6 DTO 模板（StudentDTO）

```java
package com.gp.business.student.domain;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.*;
import java.util.Date;

/**
 * 学生新增/修改入参 DTO
 * 用于接收前端请求参数
 */
@Data
@Schema(description = "学生信息入参")
public class StudentDTO {

    @Schema(description = "主键ID（修改时必传）")
    private Long id;

    @NotBlank(message = "学号不能为空")
    @Size(max = 32, message = "学号长度不能超过32个字符")
    @Schema(description = "学号", required = true)
    private String studentNo;

    @NotBlank(message = "学生姓名不能为空")
    @Size(max = 64, message = "姓名长度不能超过64个字符")
    @Schema(description = "学生姓名", required = true)
    private String studentName;

    @Pattern(regexp = "^$|^[0-2]{1}$", message = "性别值不合法")
    @Schema(description = "性别（0男 1女）")
    private String gender;

    @Schema(description = "出生日期")
    private Date birthDate;

    @Pattern(regexp = "^$|^1[3-9]\\d{9}$", message = "手机号格式不正确")
    @Schema(description = "联系电话")
    private String phone;

    @Email(message = "邮箱格式不正确")
    @Schema(description = "邮箱")
    private String email;

    @Schema(description = "班级ID")
    private Long classId;

    @Schema(description = "入学日期")
    private Date enrollDate;

    @Schema(description = "头像URL")
    private String avatar;

    @Schema(description = "家庭住址")
    private String address;

    @Schema(description = "状态（0启用 1停用）")
    private String status;

    @Schema(description = "备注")
    private String remark;
}
```

### 6.7 VO 模板（StudentVO）

```java
package com.gp.business.student.domain;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import java.util.Date;

/**
 * 学生详情/列表出参 VO
 * 用于返回给前端的数据展示
 */
@Data
@Schema(description = "学生信息出参")
public class StudentVO {

    @Schema(description = "主键ID")
    private Long id;

    @Schema(description = "学号")
    private String studentNo;

    @Schema(description = "学生姓名")
    private String studentName;

    @Schema(description = "性别（0男 1女 2未知）")
    private String gender;

    @Schema(description = "出生日期")
    private Date birthDate;

    @Schema(description = "联系电话")
    private String phone;

    @Schema(description = "邮箱")
    private String email;

    @Schema(description = "班级ID")
    private Long classId;

    @Schema(description = "班级名称")
    private String className;

    @Schema(description = "入学日期")
    private Date enrollDate;

    @Schema(description = "头像URL")
    private String avatar;

    @Schema(description = "状态（0启用 1停用）")
    private String status;

    @Schema(description = "备注")
    private String remark;

    @Schema(description = "创建者")
    private String createBy;

    @Schema(description = "创建时间")
    private Date createTime;
}
```

### 6.8 Query 模板（StudentQuery）

```java
package com.gp.business.student.domain;

import com.gp.common.core.page.PageQuery;
import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import lombok.EqualsAndHashCode;

/**
 * 学生查询条件
 * 继承 PageQuery 自带 pageNum / pageSize
 */
@Data
@EqualsAndHashCode(callSuper = true)
@Schema(description = "学生查询条件")
public class StudentQuery extends PageQuery {

    @Schema(description = "学生姓名（模糊查询）")
    private String studentName;

    @Schema(description = "学号（模糊查询）")
    private String studentNo;

    @Schema(description = "状态")
    private String status;

    @Schema(description = "班级ID")
    private Long classId;

    @Schema(description = "开始时间")
    private String beginTime;

    @Schema(description = "结束时间")
    private String endTime;
}
```

### 6.9 Convert 模板（StudentConvert）

```java
package com.gp.business.student.convert;

import com.gp.business.student.domain.Student;
import com.gp.business.student.domain.StudentDTO;
import com.gp.business.student.domain.StudentVO;
import org.mapstruct.Mapper;
import org.mapstruct.factory.Mappers;
import java.util.List;

/**
 * 学生对象转换器（MapStruct）
 * 自动生成 DTO → Entity、Entity → VO 转换代码
 */
@Mapper(componentModel = "spring")
public interface StudentConvert {

    StudentConvert INSTANCE = Mappers.getMapper(StudentConvert.class);

    /** DTO → Entity（新增/修改时使用） */
    Student dtoToEntity(StudentDTO dto);

    /** Entity → VO（查询返回时使用） */
    StudentVO entityToVO(Student entity);

    /** Entity列表 → VO列表（分页查询时使用） */
    List<StudentVO> entityListToVOList(List<Student> list);
}
```

### 6.10 Mapper 模板

```java
package com.gp.business.student.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.gp.business.student.domain.Student;
import com.gp.business.student.domain.StudentQuery;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;
import java.util.List;

/**
 * 学生 Mapper 接口
 * 继承 BaseMapper 自动获得单表 CRUD
 */
@Mapper
public interface StudentMapper extends BaseMapper<Student> {

    /**
     * 分页查询（复杂条件写在 XML 中）
     */
    IPage<Student> selectPageList(IPage<Student> page, @Param("query") StudentQuery query);

    /**
     * 查询列表（不分页）
     */
    List<Student> selectList(@Param("query") StudentQuery query);

    /**
     * 批量逻辑删除
     */
    int batchLogicDelete(@Param("ids") List<Long> ids);
}
```

### 6.11 Mapper XML 模板

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gp.business.student.mapper.StudentMapper">

    <resultMap type="com.gp.business.student.domain.Student" id="StudentResult">
        <id     property="id"           column="id"/>
        <result property="studentNo"    column="student_no"/>
        <result property="studentName"  column="student_name"/>
        <result property="gender"       column="gender"/>
        <result property="birthDate"    column="birth_date"/>
        <result property="phone"        column="phone"/>
        <result property="email"        column="email"/>
        <result property="classId"      column="class_id"/>
        <result property="className"    column="class_name"/>
        <result property="enrollDate"   column="enroll_date"/>
        <result property="avatar"       column="avatar"/>
        <result property="address"      column="address"/>
        <result property="status"       column="status"/>
        <result property="deptId"       column="dept_id"/>
        <result property="remark"       column="remark"/>
        <result property="delFlag"      column="del_flag"/>
        <result property="createBy"     column="create_by"/>
        <result property="createTime"   column="create_time"/>
        <result property="updateBy"     column="update_by"/>
        <result property="updateTime"   column="update_time"/>
    </resultMap>

    <!-- 公共字段 -->
    <sql id="selectStudentVo">
        SELECT id, student_no, student_name, gender, birth_date, phone, email,
               class_id, class_name, enroll_date, avatar, address, status, dept_id,
               remark, del_flag, create_by, create_time, update_by, update_time
        FROM biz_student
        WHERE del_flag = '0'
    </sql>

    <!-- 分页查询 -->
    <select id="selectPageList" resultMap="StudentResult">
        <include refid="selectStudentVo"/>
        <if test="query.studentName != null and query.studentName != ''">
            AND student_name LIKE CONCAT('%', #{query.studentName}, '%')
        </if>
        <if test="query.studentNo != null and query.studentNo != ''">
            AND student_no LIKE CONCAT('%', #{query.studentNo}, '%')
        </if>
        <if test="query.status != null and query.status != ''">
            AND status = #{query.status}
        </if>
        <if test="query.classId != null">
            AND class_id = #{query.classId}
        </if>
        <if test="query.beginTime != null and query.beginTime != ''">
            AND create_time &gt;= #{query.beginTime}
        </if>
        <if test="query.endTime != null and query.endTime != ''">
            AND create_time &lt;= #{query.endTime}
        </if>
        ORDER BY id DESC
    </select>

    <!-- 列表查询 -->
    <select id="selectList" resultMap="StudentResult">
        <include refid="selectStudentVo"/>
        <if test="query.studentName != null and query.studentName != ''">
            AND student_name LIKE CONCAT('%', #{query.studentName}, '%')
        </if>
        ORDER BY id DESC
    </select>

    <!-- 批量逻辑删除 -->
    <update id="batchLogicDelete">
        UPDATE biz_student SET del_flag = '2'
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
```

### 6.12 Service 模板

```java
package com.gp.business.student.service;

import com.baomidou.mybatisplus.extension.service.IService;
import com.gp.business.student.domain.*;
import com.gp.common.core.page.PageResult;
import org.springframework.web.multipart.MultipartFile;
import javax.servlet.http.HttpServletResponse;
import java.util.List;

/**
 * 学生管理 Service 接口
 * 继承 IService 自动获得单表 CRUD
 */
public interface StudentService extends IService<Student> {

    /** 分页查询 */
    PageResult<StudentVO> pageList(StudentQuery query);

    /** 列表查询（不分页） */
    List<StudentVO> list(StudentQuery query);

    /** 查询详情 */
    StudentVO getDetail(Long id);

    /** 新增 */
    void add(StudentDTO dto);

    /** 修改 */
    void update(StudentDTO dto);

    /** 删除（支持批量） */
    void delete(List<Long> ids);

    /** 修改状态 */
    void changeStatus(Long id, String status);

    /** 导出 */
    void export(HttpServletResponse response, StudentQuery query);

    /** 导入 */
    void importStudent(MultipartFile file);

    /** 下载导入模板 */
    void importTemplate(HttpServletResponse response);

    /** 校验学号是否唯一 */
    boolean checkStudentNoUnique(StudentDTO dto);
}
```

### 6.13 ServiceImpl 模板

```java
package com.gp.business.student.service.impl;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.gp.business.student.convert.StudentConvert;
import com.gp.business.student.domain.*;
import com.gp.business.student.mapper.StudentMapper;
import com.gp.business.student.service.StudentService;
import com.gp.common.core.page.PageResult;
import com.gp.common.exception.BusinessException;
import com.gp.common.utils.SecurityUtils;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import java.util.List;

/**
 * 学生管理 Service 实现类
 */
@Service
public class StudentServiceImpl
        extends ServiceImpl<StudentMapper, Student>
        implements StudentService {

    @Autowired
    private StudentMapper studentMapper;

    @Autowired
    private StudentConvert studentConvert;

    /**
     * 分页查询
     */
    @Override
    public PageResult<StudentVO> pageList(StudentQuery query) {
        Page<Student> page = new Page<>(query.getPageNum(), query.getPageSize());
        IPage<Student> result = studentMapper.selectPageList(page, query);
        List<StudentVO> voList = studentConvert.entityListToVOList(result.getRecords());
        return new PageResult<>(result.getTotal(), voList, query.getPageNum(), query.getPageSize());
    }

    /**
     * 列表查询（不分页）
     */
    @Override
    public List<StudentVO> list(StudentQuery query) {
        List<Student> list = studentMapper.selectList(query);
        return studentConvert.entityListToVOList(list);
    }

    /**
     * 查询详情
     */
    @Override
    public StudentVO getDetail(Long id) {
        Student student = studentMapper.selectById(id);
        if (student == null) {
            throw new BusinessException("学生信息不存在");
        }
        return studentConvert.entityToVO(student);
    }

    /**
     * 新增
     */
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void add(StudentDTO dto) {
        // 校验学号唯一
        if (!checkStudentNoUnique(dto)) {
            throw new BusinessException("学号已存在");
        }
        Student student = studentConvert.dtoToEntity(dto);
        studentMapper.insert(student);
    }

    /**
     * 修改
     */
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void update(StudentDTO dto) {
        Student exist = studentMapper.selectById(dto.getId());
        if (exist == null) {
            throw new BusinessException("学生信息不存在");
        }
        if (!checkStudentNoUnique(dto)) {
            throw new BusinessException("学号已存在");
        }
        Student student = studentConvert.dtoToEntity(dto);
        studentMapper.updateById(student);
    }

    /**
     * 删除（批量逻辑删除）
     */
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void delete(List<Long> ids) {
        studentMapper.batchLogicDelete(ids);
    }

    /**
     * 修改状态
     */
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void changeStatus(Long id, String status) {
        Student student = new Student();
        student.setId(id);
        student.setStatus(status);
        studentMapper.updateById(student);
    }

    /**
     * 校验学号唯一
     */
    @Override
    public boolean checkStudentNoUnique(StudentDTO dto) {
        Long id = dto.getId() == null ? -1L : dto.getId();
        LambdaQueryWrapper<Student> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(Student::getStudentNo, dto.getStudentNo());
        wrapper.ne(Student::getId, id);
        return studentMapper.selectCount(wrapper) == 0;
    }

    /**
     * 导出（通过 EasyExcel）
     */
    @Override
    public void export(HttpServletResponse response, StudentQuery query) {
        List<Student> list = studentMapper.selectList(query);
        // EasyExcel.write(response.getOutputStream(), Student.class).sheet("学生数据").doWrite(list);
        // 框架工具类 ExcelUtils.exportExcel(response, list, "学生数据");
    }

    /**
     * 导入
     */
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void importStudent(MultipartFile file) {
        // ExcelUtils.readExcel(file, Student.class) → 逐行校验并批量插入
    }

    /**
     * 下载导入模板
     */
    @Override
    public void importTemplate(HttpServletResponse response) {
        // ExcelUtils.exportExcel(response, new ArrayList<>(), "学生导入模板");
    }
}
```

> **说明**：`checkStudentNoUnique` 方法使用 MyBatis Plus 的 `LambdaQueryWrapper` 构建查询条件，`export`、`importStudent`、`importTemplate` 方法的具体实现依赖框架封装的 `ExcelUtils` 工具类（基于 EasyExcel），此处省略具体实现，开发时按框架提供的工具类调用即可。

### 6.14 Controller 模板

```java
package com.gp.business.student.controller;

import com.gp.business.student.domain.*;
import com.gp.business.student.service.StudentService;
import com.gp.common.annotation.Log;
import com.gp.common.core.page.PageResult;
import com.gp.common.core.result.Result;
import com.gp.common.enums.BusinessTypeEnum;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;
import javax.servlet.http.HttpServletResponse;
import javax.validation.Valid;
import java.util.List;

/**
 * 学生管理 Controller
 */
@Tag(name = "学生管理")
@RestController
@RequestMapping("/business/student")
public class StudentController {

    @Autowired
    private StudentService studentService;

    /** 分页查询 */
    @Operation(summary = "学生分页查询")
    @PreAuthorize("@ss.hasPermi('student:list')")
    @GetMapping("/page")
    public Result<PageResult<StudentVO>> page(StudentQuery query) {
        return Result.success(studentService.pageList(query));
    }

    /** 列表查询（不分页） */
    @Operation(summary = "学生列表")
    @PreAuthorize("@ss.hasPermi('student:list')")
    @GetMapping("/list")
    public Result<List<StudentVO>> list(StudentQuery query) {
        return Result.success(studentService.list(query));
    }

    /** 详情查询 */
    @Operation(summary = "学生详情")
    @PreAuthorize("@ss.hasPermi('student:query')")
    @GetMapping("/{id}")
    public Result<StudentVO> getInfo(@PathVariable Long id) {
        return Result.success(studentService.getDetail(id));
    }

    /** 新增 */
    @Operation(summary = "新增学生")
    @PreAuthorize("@ss.hasPermi('student:add')")
    @Log(title = "学生管理", businessType = BusinessTypeEnum.INSERT)
    @PostMapping
    public Result<Void> add(@Valid @RequestBody StudentDTO dto) {
        studentService.add(dto);
        return Result.success();
    }

    /** 修改 */
    @Operation(summary = "修改学生")
    @PreAuthorize("@ss.hasPermi('student:edit')")
    @Log(title = "学生管理", businessType = BusinessTypeEnum.UPDATE)
    @PutMapping
    public Result<Void> edit(@Valid @RequestBody StudentDTO dto) {
        studentService.update(dto);
        return Result.success();
    }

    /** 删除 */
    @Operation(summary = "删除学生")
    @PreAuthorize("@ss.hasPermi('student:delete')")
    @Log(title = "学生管理", businessType = BusinessTypeEnum.DELETE)
    @DeleteMapping("/{ids}")
    public Result<Void> remove(@PathVariable List<Long> ids) {
        studentService.delete(ids);
        return Result.success();
    }

    /** 修改状态 */
    @Operation(summary = "修改学生状态")
    @PreAuthorize("@ss.hasPermi('student:edit')")
    @Log(title = "学生管理", businessType = BusinessTypeEnum.UPDATE)
    @PutMapping("/status")
    public Result<Void> changeStatus(@RequestParam Long id, @RequestParam String status) {
        studentService.changeStatus(id, status);
        return Result.success();
    }

    /** 导出 */
    @Operation(summary = "导出学生数据")
    @PreAuthorize("@ss.hasPermi('student:export')")
    @Log(title = "学生管理", businessType = BusinessTypeEnum.EXPORT)
    @GetMapping("/export")
    public void export(HttpServletResponse response, StudentQuery query) {
        // 通过 EasyExcel 导出，详见框架工具类
        studentService.export(response, query);
    }

    /** 导入 */
    @Operation(summary = "导入学生数据")
    @PreAuthorize("@ss.hasPermi('student:import')")
    @Log(title = "学生管理", businessType = BusinessTypeEnum.IMPORT)
    @PostMapping("/import")
    public Result<Void> importStudent(@RequestPart("file") MultipartFile file) {
        studentService.importStudent(file);
        return Result.success();
    }

    /** 导入模板下载 */
    @Operation(summary = "下载导入模板")
    @GetMapping("/importTemplate")
    public void importTemplate(HttpServletResponse response) {
        studentService.importTemplate(response);
    }
}
```

### 6.15 统一异常模板

```java
package com.gp.common.exception;

/**
 * 业务异常
 * 用于 Service 层抛出业务逻辑错误
 */
public class BusinessException extends RuntimeException {

    private Integer code;
    private String message;

    public BusinessException(String message) {
        super(message);
        this.code = 500;
        this.message = message;
    }

    public BusinessException(Integer code, String message) {
        super(message);
        this.code = code;
        this.message = message;
    }

    public Integer getCode() { return code; }
    @Override
    public String getMessage() { return message; }
}
```

```java
package com.gp.common.handler;

import com.gp.common.core.result.Result;
import com.gp.common.exception.BusinessException;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.security.access.AccessDeniedException;
import org.springframework.validation.BindException;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

/**
 * 全局异常处理器
 * 捕获所有异常，统一转为 Result 返回
 */
@RestControllerAdvice
public class GlobalExceptionHandler {

    private static final Logger log = LoggerFactory.getLogger(GlobalExceptionHandler.class);

    /** 业务异常 */
    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBusinessException(BusinessException e) {
        log.error("业务异常: {}", e.getMessage());
        return Result.error(e.getCode(), e.getMessage());
    }

    /** 权限不足异常 */
    @ExceptionHandler(AccessDeniedException.class)
    public Result<Void> handleAccessDeniedException(AccessDeniedException e) {
        log.error("权限不足: {}", e.getMessage());
        return Result.error(403, "没有权限访问");
    }

    /** 参数校验异常（@RequestBody） */
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValidException(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldError().getDefaultMessage();
        log.error("参数校验失败: {}", message);
        return Result.error(message);
    }

    /** 参数校验异常（@ModelAttribute） */
    @ExceptionHandler(BindException.class)
    public Result<Void> handleBindException(BindException e) {
        String message = e.getFieldError().getDefaultMessage();
        log.error("参数绑定失败: {}", message);
        return Result.error(message);
    }

    /** 其他未捕获异常 */
    @ExceptionHandler(Exception.class)
    public Result<Void> handleException(Exception e) {
        log.error("系统异常: ", e);
        return Result.error("系统异常，请联系管理员");
    }
}
```

### 6.16 统一枚举模板

```java
package com.gp.common.enums;

import lombok.AllArgsConstructor;
import lombok.Getter;

/**
 * 系统返回码枚举
 */
@Getter
@AllArgsConstructor
public enum ResultCodeEnum {

    SUCCESS(200, "操作成功"),
    ERROR(500, "操作失败"),
    UNAUTHORIZED(401, "未认证"),
    FORBIDDEN(403, "权限不足"),
    NOT_FOUND(404, "资源不存在"),
    BAD_REQUEST(400, "请求参数错误");

    private final Integer code;
    private final String msg;
}
```

```java
package com.gp.common.enums;

import lombok.AllArgsConstructor;
import lombok.Getter;

/**
 * 业务操作类型枚举（用于日志记录）
 */
@Getter
@AllArgsConstructor
public enum BusinessTypeEnum {

    OTHER(0, "其他"),
    INSERT(1, "新增"),
    UPDATE(2, "修改"),
    DELETE(3, "删除"),
    EXPORT(4, "导出"),
    IMPORT(5, "导入"),
    LOGIN(6, "登录"),
    LOGOUT(7, "登出");

    private final Integer code;
    private final String msg;
}
```

### 6.17 前端 API 模板

```javascript
// src/api/business/student.js
import request from '@/utils/request'

// 分页查询
export function pageStudent(query) {
  return request({
    url: '/business/student/page',
    method: 'get',
    params: query
  })
}

// 列表查询（不分页）
export function listStudent(query) {
  return request({
    url: '/business/student/list',
    method: 'get',
    params: query
  })
}

// 查询详情
export function getStudent(id) {
  return request({
    url: '/business/student/' + id,
    method: 'get'
  })
}

// 新增
export function addStudent(data) {
  return request({
    url: '/business/student',
    method: 'post',
    data: data
  })
}

// 修改
export function updateStudent(data) {
  return request({
    url: '/business/student',
    method: 'put',
    data: data
  })
}

// 删除
export function delStudent(ids) {
  return request({
    url: '/business/student/' + ids,
    method: 'delete'
  })
}

// 修改状态
export function changeStudentStatus(id, status) {
  return request({
    url: '/business/student/status',
    method: 'put',
    params: { id, status }
  })
}
```

### 6.18 自定义注解模板

```java
package com.gp.common.annotation;

import com.gp.common.enums.BusinessTypeEnum;
import java.lang.annotation.*;

/**
 * 操作日志注解
 * 用于 Controller 方法上，自动记录操作日志
 */
@Target({ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface Log {

    /** 模块名称 */
    String title() default "";

    /** 业务操作类型 */
    BusinessTypeEnum businessType() default BusinessTypeEnum.OTHER;

    /** 是否保存请求参数 */
    boolean isSaveRequestData() default true;

    /** 是否保存响应结果 */
    boolean isSaveResponseData() default false;
}
```

```java
package com.gp.common.annotation;

import java.lang.annotation.*;
import java.math.BigDecimal;

/**
 * Excel 导出导入字段注解
 * 标注在 Entity 字段上，控制 Excel 导入导出的列映射
 */
@Target({ElementType.FIELD})
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface Excel {

    /** 列名称 */
    String name() default "";

    /** 排序号（从 0 开始） */
    int sort() default Integer.MAX_VALUE;

    /** 列宽（默认 16） */
    double width() default 16;

    /** 字段类型（0 导出导入，1 仅导出，2 仅导入） */
    int type() default 0;

    /** 导出时值转换表达式（如 "0=男,1=女"） */
    String readConverterExp() default "";

    /** 日期格式（如 "yyyy-MM-dd HH:mm:ss"） */
    String dateFormat() default "";

    /** 默认值（字段为空时使用） */
    String defaultValue() default "";
}
```

### 6.19 Axios 请求封装模板

```javascript
// src/utils/request.js
import axios from 'axios'
import { ElMessage, ElMessageBox } from 'element-plus'
import { useUserStore } from '@/store/modules/user'
import { getToken, removeToken } from '@/utils/auth'

// 创建 axios 实例
const service = axios.create({
  baseURL: import.meta.env.VITE_APP_BASE_API,  // 从环境变量读取
  timeout: 30000
})

// ===== 请求拦截器 =====
service.interceptors.request.use(
  config => {
    // 携带 Token
    const token = getToken()
    if (token) {
      config.headers['Authorization'] = 'Bearer ' + token
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// ===== 响应拦截器 =====
service.interceptors.response.use(
  response => {
    const res = response.data
    // 业务码非 200 → 错误
    if (res.code !== 200) {
      ElMessage.error(res.msg || '请求失败')
      // 401: Token 过期
      if (res.code === 401) {
        ElMessageBox.confirm('登录状态已过期，请重新登录', '提示', {
          confirmButtonText: '重新登录',
          cancelButtonText: '取消',
          type: 'warning'
        }).then(() => {
          removeToken()
          location.reload()
        })
      }
      return Promise.reject(new Error(res.msg || 'Error'))
    }
    return res
  },
  error => {
    ElMessage.error(error.message || '网络异常')
    return Promise.reject(error)
  }
)

export default service
```

### 6.20 业务枚举模板（StudentStatusEnum）

```java
package com.gp.business.student.enums;

import lombok.AllArgsConstructor;
import lombok.Getter;
import java.util.Arrays;

/**
 * 学生状态枚举
 * 替代魔法值，提高代码可读性
 */
@Getter
@AllArgsConstructor
public enum StudentStatusEnum {

    ENABLED("0", "启用"),
    DISABLED("1", "停用");

    private final String code;
    private final String desc;

    /** 根据 code 获取枚举 */
    public static StudentStatusEnum getByCode(String code) {
        return Arrays.stream(values())
                .filter(e -> e.getCode().equals(code))
                .findFirst()
                .orElse(null);
    }
}
```

### 6.21 模块常量模板（StudentConstant）

```java
package com.gp.business.student.constant;

/**
 * 学生模块常量
 * 集中管理模块内的魔法值
 */
public class StudentConstant {

    /** 缓存 Key 前缀 */
    public static final String CACHE_KEY_PREFIX = "biz_student:";

    /** 学号前缀 */
    public static final String STUDENT_NO_PREFIX = "STU";

    /** 默认分页大小 */
    public static final int DEFAULT_PAGE_SIZE = 10;

    private StudentConstant() {}
}
```

> **说明**：Excel 导入导出通常直接复用 Entity 类上的 `@Excel` 注解，无需单独创建 `StudentExcel` 类。如需复杂导出格式，可额外创建专门的 Excel VO 类。

---

## 七、系统开发流程

> **设计目标**：使用 GP-Framework 开发任何毕业设计，只需按以下 10 步流程执行，每步都有明确的输入和输出。

### 7.1 开发流程总览

```
Step 1        Step 2        Step 3        Step 4        Step 5
需求分析  →   ER图设计  →   建库建表  →   实体生成  →   接口开发
   │             │             │             │             │
   ▼             ▼             ▼             ▼             ▼
功能清单       数据关系图     SQL脚本       Java代码      Controller
需求文档       数据字典       初始数据       Entity等      Knife4j调试

Step 6        Step 7        Step 8        Step 9        Step 10
页面开发  →   权限配置  →   系统测试  →   论文撰写  →   部署上线
   │             │             │             │             │
   ▼             ▼             ▼             ▼             ▼
Vue页面        菜单SQL        功能测试       毕业论文       服务器部署
API联调        权限标识        接口测试       答辩PPT        域名访问
```

### 7.2 各步骤详解

#### Step 1：需求分析

**目标**：明确系统要做什么。

**输出物：**
- 功能清单表
- 用例图（UML）
- 需求规格说明文档

**操作：**
- 列出所有角色（如：管理员、教师、学生）
- 列出每个角色可用的功能
- 绘制用例图

**功能清单表模板：**

| 模块 | 功能 | 管理员 | 教师 | 学生 |
|------|------|--------|------|------|
| 学生管理 | 查询 | ✓ | ✓ | ✓（仅自己） |
| 学生管理 | 新增 | ✓ | ✗ | ✗ |
| 学生管理 | 修改 | ✓ | ✓ | ✗ |
| 学生管理 | 删除 | ✓ | ✗ | ✗ |

#### Step 2：ER 图设计

**目标**：设计数据库实体关系。

**输出物：**
- ER 图（实体关系图）
- 数据字典

**操作：**
- 识别所有实体（对应表）
- 确定实体属性（对应字段）
- 确定实体关系（一对一、一对多、多对多）
- 绘制 ER 图（推荐工具：draw.io、Navicat、PowerDesigner）

**ER 图示例（学生管理系统）：**

```
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   学院 dept   │ 1     N │   班级 class  │ 1     N │   学生 student│
│              │────────►│              │────────►│              │
│ dept_id (PK) │         │ class_id (PK)│         │ id (PK)      │
│ dept_name    │         │ class_name   │         │ student_no   │
│ parent_id    │         │ dept_id (FK) │         │ student_name │
└──────────────┘         └──────────────┘         │ class_id (FK)│
                                                  └──────────────┘
```

#### Step 3：建库建表

**目标**：将 ER 图转换为数据库表。

**输出物：**
- 建表 SQL 脚本（遵循第五部分规范）
- 初始化数据 SQL（菜单、字典、管理员账号）

**操作：**
- 按照 [第五部分](#五数据库模板) 的建表模板编写 SQL
- 每张表必须包含 7 个审计字段
- 插入菜单 SQL（配置路由和权限标识）

#### Step 4：实体生成

**目标**：创建后端 Java 代码骨架。

**两种方式：**

**方式一：代码生成器（推荐）**
- 打开代码生成器页面
- 选择数据库表
- 一键生成全部代码
- 复制到对应包目录

**方式二：手动创建（教学理解）**
- 按照第三部分"十五件套"规范手动创建
- 参考 [第六部分](#六代码模板) 的代码模板

**生成内容：**
- Entity / DTO / VO / Query
- Mapper + XML
- Service + ServiceImpl
- Controller
- Convert

#### Step 5：接口开发

**目标**：完善 Controller 接口，确保 API 正常工作。

**操作：**
- 检查 Controller 的 11 个标准接口是否完整
- 为每个接口添加 `@PreAuthorize` 权限注解
- 为关键操作添加 `@Log` 日志注解
- 在 Knife4j（`/doc.html`）中逐个测试接口
- 完善 ServiceImpl 中的业务逻辑（如校验唯一性、级联操作）

**验收标准：** 所有接口在 Knife4j 中返回正确的 `Result<T>`。

#### Step 6：页面开发

**目标**：开发前端页面。

**操作：**
- 复制 [第四部分 - 列表页模板](#42-列表页标准模板核心模板-)
- 全局替换 `Student` / `student` 为业务名
- 修改字段列表（表格列、搜索条件、表单项）
- 修改 API 导入路径
- 前后端联调

#### Step 7：权限配置

**目标**：配置菜单和按钮权限。

**输出物：**
- 菜单 SQL 脚本（插入 `sys_menu` 表）

**操作：**
- 编写菜单 SQL（目录 + 菜单 + 按钮权限）

**菜单 SQL 模板：**

```sql
-- 一级目录
INSERT INTO sys_menu VALUES (2000, '学生管理', 0, 1, 'student', NULL, '', 1, 0, 'M', '0', '0', '', 'education', 'admin', sysdate(), '', NULL, '');

-- 二级菜单
INSERT INTO sys_menu VALUES (2001, '学生列表', 2000, 1, 'index', 'business/student/index', '', 1, 0, 'C', '0', '0', 'student:list', 'user', 'admin', sysdate(), '', NULL, '');

-- 三级按钮权限
INSERT INTO sys_menu VALUES (2002, '学生新增', 2001, 1, '', '', '', 1, 0, 'F', '0', '0', 'student:add', '#', 'admin', sysdate(), '', NULL, '');
INSERT INTO sys_menu VALUES (2003, '学生修改', 2001, 2, '', '', '', 1, 0, 'F', '0', '0', 'student:edit', '#', 'admin', sysdate(), '', NULL, '');
INSERT INTO sys_menu VALUES (2004, '学生删除', 2001, 3, '', '', '', 1, 0, 'F', '0', '0', 'student:delete', '#', 'admin', sysdate(), '', NULL, '');
INSERT INTO sys_menu VALUES (2005, '学生导出', 2001, 4, '', '', '', 1, 0, 'F', '0', '0', 'student:export', '#', 'admin', sysdate(), '', NULL, '');
```

- 执行菜单 SQL
- 为角色分配菜单权限

#### Step 8：系统测试

**目标**：确保系统功能完整、无 Bug。

**测试清单：**

| 测试类型 | 内容 | 方法 |
|----------|------|------|
| 接口测试 | 所有 API 返回正确 | Knife4j / Postman |
| 功能测试 | 增删改查、导入导出 | 手动操作 |
| 权限测试 | 不同角色看到的菜单和数据不同 | 切换账号 |
| 边界测试 | 空数据、超长文本、非法参数 | 异常输入 |
| 分页测试 | 翻页、每页条数切换 | 手动翻页 |
| 并发测试 | 多人同时操作 | 模拟 |

#### Step 9：论文撰写

**目标**：撰写毕业设计论文。

**论文结构模板（通用）：**

```
第一章 绪论
  1.1 研究背景与意义
  1.2 国内外研究现状
  1.3 论文主要内容与结构

第二章 相关技术介绍
  2.1 Spring Boot 框架
  2.2 Vue.js 框架
  2.3 MySQL 数据库
  2.4 Redis 缓存
  2.5 MyBatis Plus
  2.6 本章小结

第三章 系统需求分析
  3.1 可行性分析（技术/经济/操作可行性）
  3.2 功能需求分析
  3.3 非功能需求分析
  3.4 用例图与用例描述
  3.5 本章小结

第四章 系统设计
  4.1 系统总体架构设计
  4.2 系统功能模块设计
  4.3 数据库设计（ER图 + 数据字典）
  4.4 接口设计
  4.5 安全设计
  4.6 本章小结

第五章 系统实现
  5.1 开发环境与工具
  5.2 后端核心模块实现
  5.3 业务模块实现（按模块分节）
  5.4 前端页面实现
  5.5 本章小结

第六章 系统测试
  6.1 测试环境
  6.2 功能测试用例
  6.3 测试结果分析
  6.4 本章小结

第七章 总结与展望
  7.1 全文总结
  7.2 不足与展望

参考文献
致谢
附录（核心代码）
```

#### Step 10：部署上线

**目标**：将系统部署到服务器。

**部署架构：**

```
┌─────────────── 服务器 (Linux / Windows) ───────────────────┐
│                                                            │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐               │
│  │  Nginx   │   │  MySQL   │   │  Redis   │               │
│  │  :80     │   │  :3306   │   │  :6379   │               │
│  └────┬─────┘   └──────────┘   └──────────┘               │
│       │                                                    │
│       ├──► 静态资源 (前端 dist)                              │
│       │                                                    │
│       └──► 反向代理 ──► :8080 (Spring Boot JAR)            │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**部署步骤：**
- 后端：`mvn clean package` → 上传 JAR → `nohup java -jar gp.jar &`
- 前端：`npm run build` → 上传 dist 到 Nginx 目录
- 配置 Nginx 反向代理
- 配置 HTTPS（可选）

---

## 八、可插拔业务模块

> **核心思想**：基础框架（common + framework + system）是固定的、不可修改的"底座"。所有业务功能作为独立模块插入，新增业务模块不影响框架，也不需要修改框架代码。

### 8.1 可插拔架构图

```
┌──────────────────────────────────────────────────────────┐
│                     GP-Framework 底座                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                │
│  │  common  │  │framework │  │  system  │   ← 框架核心    │
│  │ 公共基础  │  │ 安全配置  │  │ 系统管理  │     不可修改    │
│  └──────────┘  └──────────┘  └──────────┘                │
├──────────────────────────────────────────────────────────┤
│                  业务模块（可插拔区域）                      │
│                                                          │
│   ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐        │
│   │ 学生    │  │ 教师    │  │ 课程    │  │ 成绩    │  ...  │
│   │Student │  │Teacher │  │Course  │  │ Score  │        │
│   └────────┘  └────────┘  └────────┘  └────────┘        │
│     可随时增减，互不影响                                    │
└──────────────────────────────────────────────────────────┘
```

### 8.2 可插拔的实现原理

#### 8.2.1 后端包扫描自动注册

Spring Boot 默认扫描启动类所在包及其子包。只要业务模块放在 `com.gp.business` 包下，会自动被扫描注册，无需任何配置。

```
启动类: com.gp.GpApplication  (@SpringBootApplication)
    扫描范围: com.gp.*（所有子包）

    com.gp.common     → 自动扫描 ✓
    com.gp.framework  → 自动扫描 ✓
    com.gp.system     → 自动扫描 ✓
    com.gp.business
        .student      → 自动扫描 ✓（新增模块）
        .teacher      → 自动扫描 ✓（新增模块）
```

#### 8.2.2 前端动态路由

前端路由分为两部分：
- **静态路由**：登录页、404 页、首页 Dashboard（写死在路由配置中）
- **动态路由**：从后端接口获取菜单列表，动态生成路由（业务模块的页面自动加载）

```javascript
// 前端动态路由生成逻辑（store/modules/permission.js）
function generateRoutes(menus) {
  const routes = menus.filter(menu => menu.menuType === 'C')  // 只处理菜单类型
    .map(menu => ({
      path: menu.path,
      component: () => import(`@/views/${menu.component}.vue`),  // 动态导入组件
      name: menu.path,
      meta: { title: menu.menuName, icon: menu.icon }
    }))
  return routes
}
```

**新增业务页面后，无需修改前端路由配置，只需在数据库中插入菜单记录即可。**

#### 8.2.3 数据库菜单动态配置

业务模块的菜单和权限通过 `sys_menu` 表配置。新增模块时执行一段菜单 SQL 即可，不影响现有数据。

### 8.3 新增业务模块操作步骤

以新增"宠物管理"模块为例：

```
Step 1: 建表
  └─ 编写 biz_pet 建表 SQL（遵循第五部分规范）→ 执行

Step 2: 生成后端代码
  ├─ 方式一: 代码生成器一键生成
  └─ 方式二: 复制 Student 模板 → 全局替换为 Pet
     ├─ Pet.java (Entity)
     ├─ PetDTO.java / PetVO.java / PetQuery.java
     ├─ PetMapper.java + PetMapper.xml
     ├─ PetService.java + PetServiceImpl.java
     ├─ PetController.java
     └─ PetConvert.java

Step 3: 生成前端代码
  ├─ 复制 api/business/student.js → 改为 pet.js
  └─ 复制 views/business/student/index.vue → 改为 pet/index.vue

Step 4: 配置菜单
  └─ 执行菜单 SQL（插入 sys_menu 记录）

Step 5: 分配权限
  └─ 在角色管理中为角色分配"宠物管理"菜单

Step 6: 测试
  └─ Knife4j 测试接口 + 前端联调

✅ 完成！无需修改框架任何代码。
```

### 8.4 常见业务模块清单

以下业务模块可以直接套用 GP-Framework 开发：

| 业务系统 | 核心业务模块 |
|----------|-------------|
| 学生管理系统 | 学院、专业、班级、学生、教师、课程、选课、成绩 |
| 宿舍管理系统 | 楼栋、楼层、房间、床位、学生、入住登记、报修、公告 |
| 图书馆管理系统 | 图书、分类、读者、借阅、归还、罚款、入库、盘点 |
| 校园体育平台 | 场地、器材、预约、赛事、队伍、报名、成绩、公告 |
| 停车管理系统 | 车场、车位、车辆、入场、出场、计费、月卡、商户 |
| 医院管理系统 | 科室、医生、排班、患者、挂号、处方、药品、收费 |
| 物流管理系统 | 客户、货物、仓库、车辆、司机、运单、调度、结算 |
| 企业 OA 系统 | 员工、部门、考勤、请假、报销、公告、会议室、审批 |
| 酒店管理系统 | 房型、房间、客户、预订、入住、退房、结算、服务 |
| 商城系统 | 商品、分类、品牌、用户、订单、购物车、支付、物流 |
| 宠物寄养系统 | 宠物、品种、客户、寄舍、预约、入住、退宿、服务、结算 |
| 报刊订阅系统 | 报刊、分类、客户、订阅、配送、结算、统计 |

> **关键**：以上所有系统的"用户管理、角色管理、菜单管理、权限管理、日志管理、字典管理"等基础功能完全相同，只有业务模块不同。这就是 GP-Framework 的复用价值。

---

## 九、系统 UI 规范

> **设计目标**：所有业务系统保持统一的视觉风格，提升用户体验和专业感。

### 9.1 整体布局规范

```
┌──────────────────────────────────────────────────────────────┐
│  Navbar（顶部导航栏，高度60px）                                  │
│  [折叠按钮] [面包屑]              [搜索] [全屏] [消息] [用户头像] │
├────────────┬─────────────────────────────────────────────────┤
│            │  TagsView（标签页导航，高度34px）                    │
│  Sidebar   │  [首页] [学生管理] [用户管理 ×]                      │
│  侧边菜单    ├─────────────────────────────────────────────────┤
│  宽度210px  │                                                 │
│  可折叠     │                                                 │
│            │              页面主体内容区（App Main）              │
│  📊 首页    │                                                 │
│  📚 系统管理 │              [搜索栏]                             │
│   用户管理  │              [工具栏：新增|删除|导出]               │
│   角色管理  │              [数据表格]                            │
│   菜单管理  │              [分页]                               │
│  🎓 学生管理 │                                                 │
│   学生列表  │                                                 │
│   成绩管理  │                                                 │
│            │                                                 │
└────────────┴─────────────────────────────────────────────────┘
```

| 区域 | 尺寸规范 |
|------|----------|
| 顶部导航栏 | 高度 60px，白色背景 |
| 侧边菜单 | 宽度 210px（展开）/ 64px（折叠），深色背景 |
| 标签页 | 高度 34px |
| 内容区 | 灰色背景 `#f0f2f5`，白色卡片承载内容 |
| 页面边距 | 20px |

### 9.2 色彩规范

```scss
// ===== 主色调 =====
$primary-color:   #409EFF;   // 主题蓝（按钮、链接、选中态）
$success-color:   #67C23A;   // 成功绿
$warning-color:   #E6A23C;   // 警告橙
$danger-color:    #F56C6C;   // 危险红
$info-color:      #909399;   // 信息灰

// ===== 中性色 =====
$text-primary:    #303133;   // 主要文字
$text-regular:    #606266;   // 常规文字
$text-secondary:  #909399;   // 次要文字
$text-placeholder: #C0C4CC;  // 占位文字

// ===== 背景色 =====
$bg-page:         #f0f2f5;   // 页面背景
$bg-card:         #FFFFFF;   // 卡片背景
$bg-sidebar:      #304156;   // 侧边栏背景
$bg-sidebar-hover: #263445;  // 侧边栏悬停

// ===== 边框色 =====
$border-base:     #DCDFE6;   // 基础边框
$border-light:    #E4E7ED;   // 浅色边框
$border-lighter:  #EBEEF5;   // 更浅边框
$border-extra-light: #F2F6FC;// 极浅边框（表格行分隔）
```

### 9.3 字体规范

```scss
// 字体族
$font-family: 'Helvetica Neue', Helvetica, 'PingFang SC', 'Hiragino Sans GB',
              'Microsoft YaHei', Arial, sans-serif;

// 字号
$font-size-large:    18px;   // 大标题
$font-size-medium:   16px;   // 小标题
$font-size-base:     14px;   // 正文（默认）
$font-size-small:    13px;   // 辅助文字
$font-size-mini:     12px;   // 次要文字

// 行高
$line-height-base:   1.5;

// 字重
$font-weight-normal: 400;    // 常规
$font-weight-bold:   700;    // 加粗（标题）
```

### 9.4 按钮规范

| 类型 | 样式 | 使用场景 |
|------|------|----------|
| Primary（主要） | 蓝色实心 `#409EFF` | 新增、提交、确认 |
| Success（成功） | 绿色实心 `#67C23A` | 启用、通过、确认 |
| Warning（警告） | 橙色实心 `#E6A23C` | 导出、重置 |
| Danger（危险） | 红色实心 `#F56C6C` | 删除、驳回 |
| Info（信息） | 灰色实心 `#909399` | 取消 |
| Primary Plain（朴素） | 蓝色描边 | 工具栏按钮（新增/修改/删除/导出） |

**表格操作列按钮**：使用 `link` 类型（链接式按钮），紧凑美观。

```vue
<el-button link type="primary" icon="Edit" @click="handleUpdate(row)">修改</el-button>
<el-button link type="danger" icon="Delete" @click="handleDelete(row)">删除</el-button>
```

### 9.5 表格规范

| 属性 | 规范 |
|------|------|
| 斑马纹 | 启用（`stripe`） |
| 边框 | 启用（`border`） |
| 表头背景 | `#f8f8f9` |
| 表头文字 | 加粗，居中 |
| 行高 | 默认 48px |
| 内容对齐 | 默认左对齐，状态/标签居中 |
| 空数据 | 显示"暂无数据"图标 |
| 加载状态 | `v-loading` 遮罩 |
| 列溢出 | `show-overflow-tooltip` 省略号提示 |

### 9.6 表单规范

| 属性 | 规范 |
|------|------|
| label 位置 | 右侧（`label-width="90px"`） |
| 必填标记 | 红色星号 `*` |
| 校验提示 | 输入框下方红色文字 |
| 表单宽度 | 弹窗内表单默认 600px |
| 栅格布局 | 两列布局用 `el-row` + `el-col :span="12"` |
| 输入框 | 默认带 `clearable` 可清除 |
| 选择框 | 默认带 `clearable` 可清除 |

### 9.7 弹窗规范

| 属性 | 规范 |
|------|------|
| 宽度 | 新增/修改弹窗 600px，详情弹窗 800px |
| 标题 | "新增XXX" / "修改XXX" / "XXX详情" |
| 底部按钮 | 主操作（确定）在右，次操作（取消）在左 |
| 关闭方式 | 右上角 X + 底部取消按钮 + ESC 键 |
| 遮罩 | 点击遮罩不关闭（`close-on-click-modal=false`） |

### 9.8 搜索栏规范

| 属性 | 规范 |
|------|------|
| 布局 | 行内表单（`inline`） |
| 输入框宽度 | 统一 200px |
| 按钮 | 搜索（Primary）+ 重置（Default） |
| 展开收起 | 通过 `showSearch` 控制显示/隐藏 |
| 回车搜索 | 输入框 `@keyup.enter="handleQuery"` |

### 9.9 统计卡片规范

首页 Dashboard 的统计卡片统一风格：

```scss
.stat-card {
  display: flex;
  align-items: center;
  padding: 20px;

  .stat-icon {
    width: 60px;
    height: 60px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 28px;
    color: #fff;
    margin-right: 16px;
  }

  .stat-value {
    font-size: 24px;
    font-weight: 700;
    color: #303133;
    line-height: 1.2;
  }

  .stat-title {
    font-size: 14px;
    color: #909399;
    margin-top: 4px;
  }
}
```

**卡片配色方案：**

| 卡片类型 | 图标背景色 | 场景 |
|----------|------------|------|
| 蓝色卡片 | `#409EFF` | 用户数、订单数 |
| 绿色卡片 | `#67C23A` | 收入、完成数 |
| 橙色卡片 | `#E6A23C` | 待处理、待审核 |
| 红色卡片 | `#F56C6C` | 异常、过期、超时 |

### 9.10 图标规范

使用 Element Plus 内置 Icon（`@element-plus/icons-vue`），常用图标：

| 功能 | 图标 |
|------|------|
| 新增 | `Plus` |
| 修改 | `Edit` |
| 删除 | `Delete` |
| 搜索 | `Search` |
| 重置 | `Refresh` |
| 导出 | `Download` |
| 导入 | `Upload` |
| 查看 | `View` |
| 刷新 | `Refresh` |
| 展开/折叠 | `Expand` / `Fold` |

### 9.11 面包屑规范

```
首页 / 系统管理 / 用户管理
```

- 第一级固定为"首页"
- 自动根据路由 `matched` 生成
- 每级可点击跳转

### 9.12 菜单规范

| 属性 | 规范 |
|------|------|
| 层级 | 最多三级（目录 → 菜单 → 页面） |
| 图标 | 一级菜单必须有图标 |
| 折叠 | 支持侧边栏折叠/展开 |
| 高亮 | 当前路由对应的菜单高亮 |
| 权限 | 根据用户角色动态显示/隐藏 |

---

## 十、毕业设计规范

> **设计目标**：确保 GP-Framework 完全符合本科毕业设计的学术要求和软件工程规范。

### 10.1 软件工程规范对照

| 软件工程要求 | GP-Framework 如何满足 |
|-------------|----------------------|
| **需求分析** | Step 1 提供功能清单表和用例图模板 |
| **系统设计** | 第三/四部分提供完整的模块划分和页面设计 |
| **数据库设计** | 第五部分提供三范式建表规范和 ER 图 |
| **编码实现** | 第六部分提供统一的代码模板 |
| **系统测试** | Step 8 提供完整的测试清单 |
| **文档编写** | Step 9 提供论文结构模板 |

### 10.2 设计模式与架构原则

#### 10.2.1 MVC 设计思想

```
Model（模型）          View（视图）           Controller（控制器）
     │                      │                       │
     ▼                      ▼                       ▼
 Entity / DTO / VO    Vue3 前端页面          Controller 接口
 Mapper / Service     Element Plus 组件      统一入口/出口
     │                      │                       │
     └──────── 数据通过 Result<T> JSON 传递 ─────────┘
```

#### 10.2.2 三层架构

| 层 | 包名 | 职责 | 依赖方向 |
|----|------|------|----------|
| 表现层 | `controller` | 接收请求、返回响应 | → 业务层 |
| 业务层 | `service` | 业务逻辑、事务控制 | → 数据层 |
| 数据层 | `mapper` | 数据库访问 | → 数据库 |

**依赖原则：上层依赖下层，下层不感知上层。Controller 不直接操作 Mapper，必须通过 Service。**

#### 10.2.3 高内聚低耦合

| 设计 | 体现 |
|------|------|
| 高内聚 | 每个模块（包）只负责一个业务领域（如 `student` 包只处理学生相关） |
| 低耦合 | 业务模块之间不直接调用，需要时通过 Service 接口或事件机制通信 |
| 单一职责 | 每个类只做一件事（Controller 只管路由，Service 只管逻辑，Mapper 只管数据） |
| 开闭原则 | 对扩展开放（新增业务模块），对修改关闭（不改框架代码） |

#### 10.2.4 RBAC 权限模型

符合经典的 RBAC（Role-Based Access Control）权限模型：

```
用户(User) ──N:N── 角色(Role) ──N:N── 权限(Permission/Menu)
                                      ├── 页面权限（菜单可见性）
                                      ├── 接口权限（@PreAuthorize）
                                      └── 按钮权限（v-hasPermi）
```

**三级权限粒度：**
- 页面级：菜单分配，控制侧边栏显示
- 接口级：`@PreAuthorize("@ss.hasPermi('student:list')")`
- 按钮级：`v-hasPermi="['student:add']"`

### 10.3 数据库三范式

| 范式 | 要求 | GP-Framework 如何满足 |
|------|------|----------------------|
| **第一范式（1NF）** | 字段不可再分（原子性） | 每个字段都是最小单元，如地址拆分为省、市、区 |
| **第二范式（2NF）** | 非主键字段完全依赖主键 | 每张表只描述一个实体，使用自增主键 ID |
| **第三范式（3NF）** | 非主键字段直接依赖主键（消除传递依赖） | 关联数据用外键 ID 关联，而非冗余存储 |

**特殊说明（适度反范式）：**
为提升查询性能，在个别场景下允许适度冗余（如 `biz_student` 表中冗余 `class_name` 字段，避免每次 JOIN 班级表）。这种设计在毕设论文中应说明理由（性能优化），展示对范式的辩证理解。

### 10.4 毕设论文 UML 图清单

毕业设计论文中应包含以下 UML 图（GP-Framework 已提供模板）：

| 图表类型 | 论文章节 | 工具推荐 |
|----------|----------|----------|
| 用例图 | 3.4 功能需求 | draw.io / StarUML |
| ER 图 | 4.3 数据库设计 | Navicat / PowerDesigner |
| 类图 | 4.2 系统设计 | IDEA 自带 / StarUML |
| 时序图 | 5.x 系统实现（关键流程） | draw.io / PlantUML |
| 活动图 | 3.x 业务流程 | draw.io |
| 部署图 | 4.1 架构设计 | draw.io |
| 组件图 | 4.1 架构设计 | draw.io |

### 10.5 可维护性 / 可扩展性 / 可复用性

| 质量属性 | 如何实现 | 毕设答辩如何阐述 |
|----------|----------|-----------------|
| **可维护** | 统一的代码规范、统一的项目结构、完善的注释 | "系统采用统一的三层架构和命名规范，任何开发者都能快速定位代码" |
| **可扩展** | 可插拔业务模块、Spring Boot 自动扫描 | "新增功能模块只需添加业务包，无需修改框架代码，符合开闭原则" |
| **可复用** | 公共组件库、统一代码模板、代码生成器 | "框架基础模块可在多个项目中复用，通过模板快速生成业务代码" |

### 10.6 框架符合毕设要求对照表

| 毕设要求 | 达标情况 | 说明 |
|----------|----------|------|
| 选题合理 | ✓ | 后台管理系统是计算机专业经典选题方向 |
| 需求分析完整 | ✓ | Step 1 提供需求分析模板 |
| 架构设计规范 | ✓ | 前后端分离 + 三层架构 + MVC |
| 数据库设计规范 | ✓ | 三范式 + ER 图 + 数据字典 |
| 编码规范 | ✓ | 统一命名 + 统一模板 + 统一异常处理 |
| 权限控制 | ✓ | RBAC + JWT + 三级权限 |
| 功能完整 | ✓ | 完整 CRUD + 导入导出 + 统计分析 |
| 界面美观 | ✓ | Element Plus + 统一 UI 规范 |
| 测试充分 | ✓ | Step 8 提供完整测试清单 |
| 论文规范 | ✓ | Step 9 提供论文结构模板 |
| 答辩可讲解 | ✓ | 代码自主设计，非黑盒框架，可清晰阐述每行代码 |

### 10.7 框架使用声明（毕设答辩建议）

> GP-Framework 借鉴了若依（RuoYi）等业界优秀后台框架的设计思想，但所有代码均为自主编写。框架中的基础模块（用户/角色/菜单/权限等）为通用基础设施，业务模块为学生本人针对具体需求独立开发。答辩时建议：
> - 重点讲解 **业务模块** 的设计思路和实现细节
> - 说明 **RBAC 权限模型** 的三级权限控制原理
> - 展示 **数据库设计** 的三范式思考
> - 演示 **完整的功能流程**（登录 → 操作 → 查看日志）
> - 强调 **可扩展性**（现场演示新增一个模块的过程）

---

## 附录：框架快速启动检查清单

搭建一个新的毕业设计项目时，按以下清单逐项确认：

### 环境准备
- [ ] JDK 8+ 已安装
- [ ] Maven 3.6+ 已安装
- [ ] Node.js 16+ 已安装
- [ ] MySQL 8.0 已安装并创建数据库
- [ ] Redis 已安装并启动

### 后端启动
- [ ] 导入 `gp-admin` 项目到 IDEA
- [ ] 修改 `application-dev.yml` 数据库连接
- [ ] 修改 `application-dev.yml` Redis 连接
- [ ] 执行 `sql/01_gp_framework.sql`（框架基础表）
- [ ] 执行 `sql/02_gp_init_data.sql`（初始化数据）
- [ ] 执行 `sql/03_gp_business.sql`（业务表）
- [ ] 执行 `sql/04_gp_business_menu.sql`（业务菜单）
- [ ] 启动 `GpApplication`，访问 `http://localhost:8080`
- [ ] 访问 Knife4j `http://localhost:8080/doc.html`

### 前端启动
- [ ] 导入 `gp-ui` 项目到 VSCode
- [ ] `npm install`（安装依赖）
- [ ] 修改 `.env.development` 中 `VITE_APP_BASE_API`
- [ ] `npm run dev`（启动开发服务器）
- [ ] 访问 `http://localhost:80` 登录系统
- [ ] 默认账号：`admin / admin123`

### 开发新模块
- [ ] 建表（遵循第五部分）
- [ ] 后端十五件套（遵循第三部分）
- [ ] 前端 API + 页面（遵循第四部分）
- [ ] 菜单 SQL（遵循 Step 7）
- [ ] 测试 + 联调（遵循 Step 8）

---

**文档版本**：v1.0
**适用范围**：所有本科计算机专业毕业设计后台管理系统
**核心理念**：一次搭建，N 次复用 —— 新系统只需修改业务模块即可完成开发
