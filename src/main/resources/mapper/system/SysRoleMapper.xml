<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gp.system.mapper.SysRoleMapper">

    <select id="selectRolesByUserId" resultType="com.gp.system.domain.SysRole">
        SELECT r.id, r.role_name, r.role_key, r.role_sort, r.status, r.del_flag
        FROM sys_role r
        LEFT JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId} AND r.del_flag = '0'
    </select>
</mapper>
