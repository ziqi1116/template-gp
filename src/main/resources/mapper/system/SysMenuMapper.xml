<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gp.system.mapper.SysMenuMapper">

    <resultMap id="SysMenuResult" type="com.gp.system.domain.SysMenu">
        <id     property="id"         column="id"/>
        <result property="menuName"   column="menu_name"/>
        <result property="parentId"   column="parent_id"/>
        <result property="orderNum"   column="order_num"/>
        <result property="path"       column="path"/>
        <result property="component"  column="component"/>
        <result property="queryParam" column="query_param"/>
        <result property="isFrame"    column="is_frame"/>
        <result property="isCache"    column="is_cache"/>
        <result property="menuType"   column="menu_type"/>
        <result property="visible"    column="visible"/>
        <result property="status"     column="status"/>
        <result property="perms"      column="perms"/>
        <result property="icon"       column="icon"/>
    </resultMap>

    <sql id="selectMenuVo">
        SELECT id, menu_name, parent_id, order_num, path, component, query_param,
               is_frame, is_cache, menu_type, visible, status, perms, icon
        FROM sys_menu WHERE status = '0' AND del_flag = '0'
    </sql>

    <select id="selectPermsByUserId" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND m.perms IS NOT NULL AND m.perms != ''
    </select>

    <select id="selectMenusByUserId" resultMap="SysMenuResult">
        SELECT DISTINCT m.id, m.menu_name, m.parent_id, m.order_num, m.path, m.component,
               m.query_param, m.is_frame, m.is_cache, m.menu_type, m.visible, m.status, m.perms, m.icon
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND m.menu_type IN ('M', 'C') AND m.status = '0' AND m.del_flag = '0'
        ORDER BY m.parent_id, m.order_num
    </select>

    <select id="selectMenuTreeAll" resultMap="SysMenuResult">
        <include refid="selectMenuVo"/>
        AND menu_type IN ('M', 'C')
        ORDER BY parent_id, order_num
    </select>
</mapper>
